# 2026-06-24 开发日志

## 当日完成

- 继续排查菜单栏应用入口不可见问题：补充运行时诊断日志，确认原生 `NSStatusItem` 曾被放到屏幕外 `frame=(0, -6/-17, 48, 22)`，不是应用被系统关闭
- 根据用户反馈撤销自绘 fallback 入口方案：该方案会造成自动化点击可弹出、用户肉眼不可见的假阳性，不能作为修复结果
- 将菜单栏入口改回纯原生 `NSStatusItem.squareLength` + `imageOnly`，不再设置 `autosaveName`，避免复用 macOS 保存的隐藏/位置状态
- 保留 0.5s / 2s / 5s 延迟可见性日志，记录 `button.window`、屏幕坐标、`statusItem.isVisible` 和 `length`，用于判断是否仍被系统放到不可见区域
- 重新执行 OneBoard 打包流程，生成 `build/OneBoard.dmg`
- 使用 ad-hoc 签名方式完成本地安装包打包：`ONEBOARD_CODESIGN_IDENTITY=- script/package_app.sh`
- 执行 `hdiutil verify build/OneBoard.dmg`，确认 DMG 校验通过
- 新增 `docs/打包与修复流程/README.md`，记录标准打包方法和修 bug 闭环流程
- 更新 `AGENTS.md` 和 `CLAUDE.md`，加入打包与修复流程入口，确保后续任务优先阅读

## 验证

- `HOME=/private/tmp/oneboard-home CLANG_MODULE_CACHE_PATH=/private/tmp/oneboard-module-cache swift build --disable-sandbox`：编译通过
- 直接启动旧构建并捕获日志：确认原生状态项不可见、fallback 面板 `frame=(1040, 934, 58, 22)`、`isVisible=true`；但该验证不能证明用户可见
- 用户复测反馈：用户侧无法看到菜单栏图标，只有自动化点击后短暂出现/弹出，因此 fallback 方案判定失败
- `defaults read com.oneboard.app`：`NSStatusItem Visible OneBoardStatusItem = 1`，系统偏好层未显式关闭 OneBoard
- `defaults read com.apple.controlcenter`：`NSStatusItem Visible OneBoardStatusItem = 1`，Control Center 偏好层未显式关闭 OneBoard
- `defaults read com.jordanbaird.Ice`：存在历史 Ice 配置，包含 `AutoRehide=1`、`ShowOnClick=1`、`UseIceBar=1`；但当前进程列表未发现 Ice/Bartender/Hidden Bar 等菜单栏管理器运行
- `ps axww ...`：当前只发现 `ControlCenter` 和 `SystemUIServer`，未发现第三方菜单栏隐藏工具进程
- `HOME=/private/tmp/oneboard-home CLANG_MODULE_CACHE_PATH=/private/tmp/oneboard-module-cache swift build --disable-sandbox`：撤销 fallback 后编译通过
- `ONEBOARD_CODESIGN_IDENTITY=- script/build_app_bundle.sh`：重新生成并签名 `build/OneBoard.app` 成功
- `codesign --verify --deep --strict --verbose=2 build/OneBoard.app`：校验通过
- `ONEBOARD_CODESIGN_IDENTITY=- script/package_app.sh`：重新生成 `build/OneBoard.dmg` 成功
- `hdiutil verify build/OneBoard.dmg`：校验通过，checksum 有效
- 当前 Codex 环境无法完成最终肉眼可见验证：沙盒外 GUI 启动被额度系统拒绝，沙盒内 `open` 返回 LaunchServices `kLSNoExecutableErr`
- `script/package_app.sh`：release 构建通过，但本机自动选择的签名身份不可用，`codesign` 报 `no identity found`
- `ONEBOARD_CODESIGN_IDENTITY=- script/package_app.sh`：ad-hoc 签名打包成功，生成 `build/OneBoard.dmg`
- `hdiutil verify build/OneBoard.dmg`：校验通过
- `script/package_app.sh`：沙盒内 `hdiutil create` 报“设备未配置”
- `ONEBOARD_CODESIGN_IDENTITY=- script/package_app.sh`：沙盒外执行成功，生成 `build/OneBoard.dmg`
- `hdiutil verify build/OneBoard.dmg`：校验通过

## 备注

- 当前安装包为本地 ad-hoc 签名安装包，适合本地测试和手动安装
- 若后续正式发布，需要配置有效 Developer ID 签名和公证流程
- 本问题当前不能判定修复完成；下一步必须在用户本机正常启动新构建后，确认菜单栏静止状态下是否持续显示原生图标
