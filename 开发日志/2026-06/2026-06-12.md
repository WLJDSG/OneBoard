# 开发日志 - 2026-06-12

## 今日完成

### 截图复制/贴图空白画布修复

- [x] **复制后图片偏到左下角**：修复标注渲染时底图只按显示尺寸绘制的问题；当输出 bitmap 保留原图像素尺寸时，底图同步按像素尺寸铺满，避免右上方出现透明/空白区域。
- [x] **PNG 导出稳定性**：`NSImage.pngData` / `jpegData` 优先复用已有 `NSBitmapImageRep`，避免重新生成 `CGImage` 时丢失已渲染 bitmap 的像素表示。
- [x] **回归测试**：新增 PNG 导出回归用例，覆盖渲染 bitmap 和最终 PNG 数据的像素尺寸、边角内容，防止复制/保存/贴图再次出现空白画布。
- [x] **重新打包**：执行 `script/package_app.sh` 重新生成 `build/OneBoard.dmg`，产物更新时间为 2026-06-12 00:36。

### 截图文字标注输入焦点修复

- [x] **文字输入无法输入**：将截图标注画布窗口改为可成为 key/main 的 `AnnotationPanel`，避免 borderless `NSPanel` 导致 SwiftUI `TextField` 假聚焦但无法接收键盘输入。
- [x] **关闭卡住**：关闭标注窗口前主动结束 first responder，并保持工具栏与画布窗口一起关闭，减少进入文字输入状态后的窗口残留。
- [x] **焦点时机稳定性**：文字输入和文字编辑浮层出现后延后一轮主队列设置焦点，确保窗口已完成 key window 切换。

### 隐私授权清除入口

- [x] **菜单入口**：菜单栏新增「清除隐私授权...」，用户确认后执行授权清除。
- [x] **授权重置**：通过系统 `tccutil reset Accessibility/ScreenCapture com.oneboard.app` 清除 OneBoard 的辅助功能和屏幕录制授权记录。
- [x] **结果提示**：清除成功或失败后显示系统提示，避免静默修改隐私状态。
- [x] **重新打包**：执行 `script/package_app.sh` 重新生成 `build/OneBoard.dmg`，产物更新时间为 2026-06-12 01:00。

### 截图文字标注交互优化

- [x] **点击画布提交文字**：文字输入完成后点击图片其他区域，会提交为纯文字并隐藏输入边框。
- [x] **输入框控制样式**：文字输入态显示边框和 8 个控制点，贴近截图工具的文本框样式。
- [x] **移动与缩放**：已提交的文字在光标工具或文字工具下可拖动移动，选中后可通过边缘和角点调整文本框尺寸。
- [x] **重新打包**：执行 `script/package_app.sh` 重新生成 `build/OneBoard.dmg`，产物更新时间为 2026-06-12 01:16。

### 文件暂存区误触发修复

- [x] **限制触发条件**：拖拽检测器记录 `.drag` pasteboard 的 `changeCount`，只有新的文件拖拽数据才会确认拖拽会话，避免普通鼠标按住晃动读取到旧拖拽数据后唤出暂存区。
- [x] **非文件拖拽清理**：非文件拖拽或旧拖拽数据不会累积摇晃轨迹，松开鼠标后同步重置当前拖拽状态。
- [x] **回归测试**：新增 DragDetector 用例，覆盖旧 `changeCount` 的文件类型不会被确认为当前文件拖拽。

## 验证

- [x] `swift test` 通过（4 tests, 0 failures）
- [x] `swift build` 通过
- [x] `script/package_app.sh` 通过，生成 `build/OneBoard.dmg`
- [x] `swift test --filter DragDetectorTests` 通过（2 tests, 0 failures）
- [x] `swift build` 通过
