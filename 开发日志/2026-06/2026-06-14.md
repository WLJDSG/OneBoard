# 开发日志 - 2026-06-14

## 今日完成

- [x] **重新打包**：执行 `script/package_app.sh` 重新生成 `build/OneBoard.dmg`，产物更新时间为 2026-06-14 03:01。
- [x] **翻译浮框工作台**：新增独立翻译浮框，支持原文编辑、译文查看、当前浮框内源语言/目标语言选择、语言交换、手动重新翻译和复制译文。
- [x] **翻译入口统一**：截图翻译和选中文字翻译统一使用新浮框，打开后按设置页默认语言自动首译一次；浮框内语言选择仅对当前浮框生效，不写回默认设置。
- [x] **翻译浮框拖拽修复**：翻译浮框改为仅顶部标题栏可拖拽，正文、语言选择和按钮区域不参与拖拽。
- [x] **翻译服务升级**：翻译浮框升级为工作台，可在 Apple、Google、DeepSeek 间切换；Google 使用网页版接口，DeepSeek 继续使用 API Key，Apple 在 macOS 26+ 走系统 Translation。
- [x] **剪贴板体验修复**：长文本悬浮时固定单行省略，避免操作按钮出现后换行；搜索栏改为点击后聚焦输入。
- [x] **文件暂存区视觉优化**：放大暂存窗口、文件卡片和缩略图；关闭按钮与单文件删除按钮改为更清晰协调的圆形控件。
- [x] **重新打包**：执行 `script/package_app.sh` 重新生成 `build/OneBoard.dmg`，并挂载检查 DMG 内容。
- [x] **回归修复**：修复翻译窗口内容裁切、Apple Translation 会话接入、剪贴板搜索栏无法输入、暂存区文件删除按钮圆形显示不完整的问题。
- [x] **截图文字标注修复**：修复截图工作台贴文字后导出/贴图中文字上下左右颠倒的问题。
- [x] **翻译工作台优化**：优化翻译浮框标题栏视觉；Apple Translation 自动检测时先做本地轻量语言推断，减少系统无法识别源语言弹窗。
- [x] **剪贴板搜索修复**：搜索改为中文友好的子串模糊匹配，支持按 `18`、`测试`、`通过` 等关键词命中同一条中文内容。
- [x] **剪贴板点击粘贴修复**：剪贴板浮窗打开时记录原前台应用，点击条目后恢复目标应用再模拟 Cmd+V。
- [x] **重新打包**：重新生成 `build/OneBoard.dmg`。
- [x] **网关切换集成设计**：梳理 `gateway-switch` 项目能力，确定完全并入 OneBoard，第一版包含网关/DNS Profile、专用切换小窗、`Cmd+Shift+G` 可配置快捷键、OneBoard 专属免密 helper。
- [x] **授权体系设计调整**：规划新增独立“授权”设置页，集中管理辅助功能、屏幕录制、网关 helper、开机自启，并将菜单项升级为 `清除 OneBoard 授权...`。
- [x] **权限联动问题纳入范围**：确认清除授权和关闭权限后文案/开关不同步的问题，设计统一系统能力状态刷新通知和清除全部授权流程。
- [x] **文档补充**：新增网关集成设计文档，更新 README 和开发步骤中的网关规划。
- [x] **网关切换模块实现**：新增 `OneBoard/Modules/Gateway/`，实现网关 Profile、当前网络检测、网关 + DNS / 仅 DNS 切换、OneBoard 专属免密 helper 管理和白名单同步。
- [x] **网关 UI 接入**：新增网关切换小窗、设置页“网关”Tab、`Cmd+Shift+G` 可配置快捷键和菜单栏 `网关切换...` 入口。
- [x] **授权页面调整**：新增设置页“授权”Tab，集中管理辅助功能、屏幕录制、网关 Helper、开机自启；菜单项升级为 `清除 OneBoard 授权...`，支持仅清隐私权限或清除全部授权。

## 验证

- [x] `script/package_app.sh` 通过，生成 `build/OneBoard.dmg`
- [x] `hdiutil verify build/OneBoard.dmg` 通过，镜像 checksum 有效
- [x] `swift test --filter TranslationPanelViewModelTests` 通过（10 个测试）
- [x] `swift test` 通过（15 个测试）
- [x] `swift build` 通过
- [x] `swift test --filter TranslationPanelViewModelTests` 通过（13 个测试）
- [x] `swift test` 通过（18 个测试）
- [x] `swift build` 通过
- [x] `script/package_app.sh` 通过，生成 `build/OneBoard.dmg`
- [x] `hdiutil verify build/OneBoard.dmg` 通过，checksum 有效
- [x] `swift test --filter Gateway` 通过（14 个网关相关测试）
- [x] `swift test` 通过（36 个测试）
- [x] `swift build` 通过
- [x] `hdiutil attach build/OneBoard.dmg -readonly` 通过，DMG 内包含 `OneBoard.app` 和 `Applications` 链接
- [x] `swift test` 通过（18 个测试）
- [x] `swift build` 通过
- [x] `script/package_app.sh` 通过，重新生成 `build/OneBoard.dmg`
- [x] `hdiutil verify build/OneBoard.dmg` 通过，checksum 有效
- [x] `swift build --disable-sandbox` 通过
- [x] `swift test --disable-sandbox` 通过（22 个测试）
- [x] `script/package_app.sh` 通过，生成 `build/OneBoard.dmg`
- [x] `hdiutil verify build/OneBoard.dmg` 通过，checksum 有效
