# 开发日志 - 2026-06-04

## 今日完成

### v1.0.7 体验修复
- [x] **权限引导**：移除 App 内 NSAlert 弹窗，开启权限时直接打开对应系统权限页，并显示无标题栏磨砂引导浮窗
- [x] **权限状态同步**：授权成功后自动关闭权限引导浮窗并回到设置页；手动移除系统权限后，设置页开关会自动刷新为关闭
- [x] **截图默认行为**：新增“移动”工具并设为默认，打开截图后不会直接进入矩形绘制；拖动截图本体可移动截图窗口
- [x] **截图快捷键**：Esc 可关闭截图标注窗口；截图工具栏移除“格式”式样，文字工具改为输入光标图标
- [x] **截图保存和贴图**：保存面板置前显示；贴图窗口改为纯图片无边框；钉住后关闭原截图标注窗口
- [x] **马赛克增强**：加大马赛克块和遮挡强度，提升内容遮挡效果
- [x] **文件暂存**：去除顶部小黑条、右上角下拉箭头、底部半圆按钮；暂存窗口可拖动且内部文件仍可拖出
- [x] **拖拽唤醒**：在 CGEventTap 外新增高频轮询兜底，拖拽文件摇晃或进入顶部触发区都可尝试唤出暂存区

### v1.0.8 权限与截图焦点修复
- [x] **权限流程**：修复设置页定时同步反复激活 App 的问题，避免截图后抢走其他应用输入焦点
- [x] **权限完成态**：区分授权流程和移除流程；授权/移除完成后关闭系统设置和悬浮引导，再回到 OneBoard 设置页
- [x] **屏幕权限**：开启屏幕权限时不再因为其他权限已授权而提前关闭引导
- [x] **截图移动**：移动工具拖拽时持续更新拖拽基准点，修复截图窗口无法调整位置
- [x] **暂存唤醒**：轮询兜底不再因鼠标进入顶部区域而误触，仅在拖拽内容或已有拖拽轨迹时参与判断

### v1.0.9 截图标注与暂存区打磨
- [x] **截图标注**：工具栏贴近截图下方，移除文字工具；Esc 本地/全局按键兜底关闭截图标注窗口
- [x] **贴图窗口**：贴图改为纯图片窗口，双击图片直接关闭自身窗口
- [x] **OCR/翻译**：结果改为右侧磨砂悬浮窗展示，保持和截图工具栏一致的轻量风格
- [x] **箭头/直线**：绘制时固定第一次点击的起点，拖动期间只调整终点，避免起点乱飘
- [x] **马赛克**：改为更密集、更随机的遮挡纹理，提高遮挡强度
- [x] **文件暂存**：改为圆角磨砂透明悬浮框，文件以网格平铺展示，每个文件右上角可单独删除
- [x] **文件拖拽**：窗口拖拽区域和文件拖出区域分离，避免拖文件时暂存窗口跟着移动
- [x] **重复暂存**：对暂存文件做标准路径去重，避免从暂存区拖回时重复添加
- [x] **菜单栏图标**：改为透明底系统模板图标，去除蓝色底色

### v1.0.11 截图架构重构与多项UI修复
- [x] **截图流程重构**：完全替换系统 screencapture -i 命令，改为「冻结全屏 → 自定义框选 → 恢复屏幕」方案，避免动态内容导致截图失败
- [x] **截图窗口定位**：框选完成后窗口直接定位在选区位置，不再偏移到其他位置
- [x] **工具栏独立悬浮**：工具栏从截图窗口内部分离为独立 NSPanel，位于截图窗口下方 0.5cm；图片窗口和工具栏窗口移动联动
- [x] **拖拽顺滑优化**：将 SwiftUI DragGesture 替换为 NSEvent addLocalMonitorForEvents，窗口拖拽更流畅
- [x] **工具栏边界自适应**：工具栏超出屏幕右侧时自动左移，超出左侧时右移，保证完整显示
- [x] **颜色选择器重构**：8个色块收进一个颜色按钮，点击弹出预设颜色 + 系统 RGB 选色面板；截图默认颜色改为红色
- [x] **文字标注工具**：替换高亮笔工具为文字标注；点击文字图标 → 点击截图 → 弹出输入框 → 确认后生成文字图层；双击可编辑，可拖拽移动，Delete 删除，支持多个
- [x] **OCR/翻译弹窗位置**：在截图窗口右侧 1cm 处显示；右侧超出则自动切换到左侧
- [x] **辅助功能权限开关**：设置页新增辅助功能权限开关，和屏幕录制权限开关逻辑一致
- [x] **摇晃检测优化**：降低检测阈值（速度 100px/s / 位移 3px），提升轮询频率至 30ms，放宽触发条件
- [x] **剪贴板弹窗**：去掉标题栏/关闭按钮/底部状态栏，改为无边框磨砂面板；光标离开窗口自动关闭
- [x] **权限关闭流程**：revoke 模式下等用户手动关闭系统设置后再回到 App 设置；修复设置页跟随关闭的 bug
- [x] **权限开关状态**：流程完成后通知设置页强制刷新开关状态和文案，避免延迟更新

### v1.0.10 UI 细节修复
- [x] **暂存区圆角**：修复暂存区悬浮框四个角有直角底色的样式问题；SwiftUI 层使用 clipShape + background 分离实现圆角和阴影，NSHostingView 层设置 cornerRadius + masksToBounds 兜底
- [x] **暂存区流畅度**：添加 `pendingFilePaths` 集合防重复竞态；窗口 resize 关闭动画（animate:false）避免卡顿
- [x] **权限关闭流程**：关闭授权开关时仅打开系统权限页，不再弹出悬浮引导窗；轮询检测权限移除后自动关闭系统设置并回到 App 设置页
- [x] **截图窗口拖拽**：修复 AnnotationViewModel.startDrawing 在 cursor 模式下不设 isDrawing 导致每帧重置拖拽基准点的 bug
- [x] **截图窗口定位**：在截图开始前记录鼠标位置，窗口优先定位在框选区域附近而非截图完成后的鼠标位置
- [x] **工具栏自适应**：工具栏改为横向 ScrollView 包装，移除 fixedSize() 约束，窄窗口下可横向滚动保证按钮完整显示

### v1.0.12 截图/暂存/权限/粘贴 多项修复 (第一轮)
- [x] **截图不可用**：`dlsym(RTLD_DEFAULT, CGDisplayCreateImage)` 在 macOS 15+ 找不到符号，改用 `CGWindowListCreateImage` + `screencapture` 回退；修复 NSImage Retina 坐标计算
- [x] **暂存区误触发**：移除 `public.utf8-plain-text` / `public.url` 类型匹配，仅文件/图片类型拖拽才检测；轮询条件改为 `isLeftMouseDown && isDraggingSupportedContent`
- [x] **剪贴板焦点**：新增全局鼠标点击监听 + `NSApplication.didResignActiveNotification` 监听，外部点击/切应用自动关闭
- [x] **权限开关 UI**：toggle getter 移除 `isOn.wrappedValue || isGranted` OR 逻辑，开关直接反映用户意图；文案始终显示实际权限状态
- [x] **设置页关闭**：revoke 模式下 `finishFlow()` 不再调用 `SettingsWindowManager.shared.show()`

### v1.0.13 截图/粘贴/权限 第二轮修复
- [x] **截图复修**：发现 `CGWindowListCreateImage` 在 macOS 15+ SDK 标记为 unavailable（过时），`screencapture` 改为主方案，使用 `CGMainDisplayID()` 替换硬编码 `-D 1`
- [x] **文件粘贴**：`readRawData` 文件类型优先通过 `pasteboard.readObjects(forClasses: [NSURL.self])` 解析文件引用 URL（如 `file:///.file/id=xxxx`）为实际路径；`writeToPasteboard` 兼容路径/URL 双格式
- [x] **验证**：`screencapture -D <displayID>` 截图成功；文件粘贴读→写回环测试通过

### v1.0.14 用户反馈修复 (第三轮)
- [x] **截图完全无反应**：系统设置 URL scheme 在 macOS 14+ 失效改用 `Process("open")`；引导悬浮窗在打开系统设置之前显示；`showOverlay` 增加鼠标所在屏幕/多屏幕回退链
- [x] **权限开关闪退**：`PermissionGuideWindowManager` 增加 `generation` 计数器防过期回调 + 1.5s 最短轮询延迟防 API 缓存误判
- [x] **摇晃暂存区**：`CGEventTap` 创建失败时延迟 2s 自动弹出权限引导

### v1.0.15 更多反馈修复 (第四轮)
- [x] **摇晃暂存区需热键先触发**：根因 `FileStagingViewModel.shared` 是 Swift `static let` 懒加载，按热键前未初始化，摇动通知无人监听。修复：`AppDelegate` 中 `_ = FileStagingViewModel.shared` 强制初始化为 observer
- [x] **窗口拖动误触发暂存区**：`handleCGEvent` / `handleMouseEvent` 在处理 `leftMouseDragged` 前增加 `isDraggingSupportedContent` 守卫，过滤所有窗口拖动
- [x] **设置页关闭**：移除 `forceRefresh = UUID()` + `.id(forceRefresh)` 方案（重建视图树导致窗口关闭），改用 `@State` 变量跟踪权限实际状态
- [x] **screencapture 诊断**：移除 `-D <id>` 参数避免 displayID 兼容问题；增加 stderr pipe 捕获错误输出

## 验证

- [x] `swift build` 通过
- [x] `script/package_app.sh` 仅生成 `build/OneBoard.dmg`
- [x] App 启动无 crash

### v1.0.16 截图架构最终重构：系统回退方案
- [x] **全新截图策略**：「全屏截图 + 自定义蒙版」优先，失败自动回退 `screencapture -i`
  - 方案一：`screencapture -t png -x /tmp/freeze.png` 全屏截图 → 自定义蒙版框选 → 裁剪
  - 方案一失败时自动回退方案二：`screencapture -i` 系统原生交互式截图工具
- [x] **代码精简**：移除已过时的 `CGWindowListCreateImage` 回退分支（macOS 15+ 不可用）
- [x] **系统截图回退**：`screencapture -i` 调起原生框选 UI，完成后读取截图文件
- [x] **选区定位**：系统截图不回传选区坐标，以图片尺寸在屏幕居中定位标注窗口

### v1.0.17 截图/权限/暂存区 深度修复
- [x] **窗口拖动误触发**：移除 `isDraggingSupportedContent` 中 `localizedCaseInsensitiveContains("file")` 模糊匹配，仅用精确 UTI + `canReadObject([NSURL])` 兜底
- [x] **截图完全无反应**：`captureFullScreen()` 改为 `Task.detached` 后台执行；移除 `CGPreflightScreenCaptureAccess` 预检；系统回退前 `NSApp.activate` + sleep 0.1s
- [x] **权限开关闪退**：根因 `finishFlow()` → `closeSystemSettings()` 杀刚打开的系统设置；修复：完全移除强制关闭，轮询延迟 1.5s → 2.0s
- [x] **设置页关闭**：根因 accessory app 失活后设置窗口被隐藏；修复：新增 `bringToFront()`，`finishFlow()` 无条件恢复
- [x] **权限代码优化**：移除 `prompt*()` / `disable{}` 中重复 `openSettings()` 调用；精简闭包
- [x] **截图代码精简**：154 行，全屏/系统回退职责清晰

- [x] App 启动无 crash

### v1.0.18 彻底重写截图/暂存区检测/权限流程

- [x] **截图彻底重写**（154 行 → 44 行）：
  - 移除所有自定义蒙版、全屏冻结、权限预检
  - 纯 `screencapture -i`，Process 在 `Task.detached` 后台线程执行
  - 不抢焦点、不阻塞主 run loop

- [x] **拖拽检测彻底重写**（247 行 → 193 行）：
  - 新增 `lastPasteboardChangeCount` 粘贴板变更计数防过期数据
  - 移除所有模糊 UTI 匹配，只用精确匹配
  - 触发冷却 1.5s

- [x] **权限流程彻底重写**（313 行 → 160 行）：
  - 移除 `generation`/`closeSystemSettings`/重复 `openSettings`
  - 移除 `PermissionGuideView` revokeMode 参数
  - `finishFlow()` 统一为 hide → bringToFront + 通知

## 验证

- [x] `swift build` 通过（零 warning）
- [x] `script/package_app.sh` 仅生成 `build/OneBoard.dmg`
- [x] App 启动无 crash

### v1.0.19 截图拖动卡顿 / 工具栏无响应 / 权限切换 bug 修复

- [x] **截图窗口拖动卡顿**：
  - KVO 回调用 `MainActor.assumeIsolated` 替代 `Task { @MainActor }`（每帧创建新 Task 造成调度风暴）
  - `setFrameOrigin` 包装 `NSAnimationContext(duration:0)` 禁用隐式动画

- [x] **工具栏点击无反应**：
  - 工具栏 level 提升到 `floatingWindow+1` 确保在图片窗口之上
  - 改用 `orderFrontRegardless()`（nonactivatingPanel 无法 makeKey）
  - 两窗口都加 `.ignoresCycle`

- [x] **权限文案不更新 / 引导窗不出现**：
  - Toggle action 中先调 `show()` 设置 `hasActiveFlow`，再更新 `@AppStorage`
  - 消除定时器与用户切换间的竞态窗口
  - `FloatingWindowManager` 兼容 `NSScreen.main` 为 nil

## 验证

- [x] `swift build` 通过（零 warning）
- [x] `script/package_app.sh` 仅生成 `build/OneBoard.dmg`
- [x] App 启动无 crash
