# 开发日志 - 2026-06-10

## 今日完成

### 截图与暂存区回归修复

- [x] **暂存区误触发**：拖拽检测只接受文件 URL / Finder 文件列表类型，移除图片 PNG/TIFF 等非文件拖拽类型匹配，并将 NSPasteboard NSURL 读取限制为 file URL；同时移除顶部区域快捷触发，只有“左键按住 + 拖动文件 + 摇晃”才唤出暂存区。
- [x] **截图复制/贴图画质下降**：标注渲染改用原图 bitmap representation 的真实像素尺寸，保留 Retina 图片像素密度，复制、保存、贴图共用同一高质量 PNG 输出。
- [x] **截图框选标注位置偏移**：渲染标注时显式把 SwiftUI 顶部原点坐标映射到 CoreGraphics bitmap 像素坐标，保证方框等标注复制/贴图后位置不变。
- [x] **截图复制/贴图尺寸位置回弹**：复制、保存、贴图按当前标注窗口显示尺寸渲染，底层仍保留原图像素；贴图窗口沿用标注窗口 frame，不再回到原图尺寸或重新居中。
- [x] **回归测试**：新增 `OneBoardTests`，覆盖文件拖拽类型过滤、Retina 图片像素尺寸保留、标注矩形坐标映射。

## 验证

- [x] `swift test` 通过（3 tests, 0 failures）
- [x] `swift build` 通过
