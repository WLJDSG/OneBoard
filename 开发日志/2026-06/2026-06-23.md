# 开发日志 - 2026-06-23

## 今日完成

- [x] **菜单栏入口修复**：修复 `MenuBarManager.setup()` 缺失闭合大括号的问题，恢复菜单栏图标点击处理方法的正确作用域。

## 验证

- [x] `env CLANG_MODULE_CACHE_PATH=/private/tmp/oneboard-clang-cache SWIFTPM_CACHE_PATH=/private/tmp/oneboard-swiftpm-cache swift build --disable-sandbox` 编译通过。

## 备注

- 普通 `swift build` 在 Codex 沙箱中因 SwiftPM/Clang 用户缓存和 SwiftPM 二次沙箱限制失败；使用临时缓存目录并关闭 SwiftPM 内层沙箱后验证通过。

## 重新打包

- [x] 执行 `script/package_app.sh`，完成 release 构建、`.app` 组装、LaunchAtLogin helper 注入和 ad-hoc 签名。
- [x] 沙箱内 `hdiutil create` 报“设备未配置”后，单独提升权限创建 `build/OneBoard.dmg`。

## 打包验证

- [x] `hdiutil verify build/OneBoard.dmg` 通过，checksum 有效。
- [x] `codesign --verify --deep --strict build/OneBoard.app` 通过。
- [x] 主 App Bundle ID：`com.oneboard.app`。
- [x] Login helper Bundle ID：`com.oneboard.app-LaunchAtLoginHelper`。

## 菜单栏入口可见性修复

- [x] 根据 `/tmp/oneboard_diag.txt` 确认 `NSStatusItem` 实际已创建，问题不是启动链路未执行。
- [x] 将菜单栏状态项改为 `variableLength`，使用系统模板图标，并增加 `OB` 文本兜底，避免图标绘制不可见时完全找不到入口。
- [x] 移除显式 `autosaveName`，避免 macOS 恢复之前隐藏/移除过的状态栏项状态。
- [x] 重新执行 `script/package_app.sh` 完成 release 构建、App bundle 签名和校验；沙箱内 `hdiutil create` 仍报“设备未配置”，已单独提升权限生成 `build/OneBoard.dmg`。

## 本轮补充验证

- [x] `env CLANG_MODULE_CACHE_PATH=/private/tmp/oneboard-clang-cache SWIFTPM_CACHE_PATH=/private/tmp/oneboard-swiftpm-cache swift build --disable-sandbox` 编译通过。
- [x] `hdiutil verify build/OneBoard.dmg` 通过，checksum 有效。
- [x] `codesign --verify --deep --strict build/OneBoard.app` 通过。
- [x] 提升权限启动 `/private/tmp/OneBoardLaunchTest-20260623-2159.app` 后，诊断日志显示菜单栏项 `title:  OB`、`hasImage: true`、`isVisible: true`。

## 菜单栏入口继续排查

- [x] 排除第三方菜单栏管理器 Ice 影响：用户确认未启动 Ice；后续诊断不再按 Ice 干扰处理。
- [x] 通过 ControlCenter 统一日志确认：OneBoard 已创建 `NSStatusItem`，且 `clientRequestsVisibility: true`，但系统随后将 `com.oneboard.app-OneBoardStatusItem` 移入 blocked list。
- [x] 发现旧偏好里仍有 `NSStatusItem VisibleCC Item-0 = 0`；修复为创建 status item 后立即设置稳定 `autosaveName = OneBoardStatusItem`，并在启动时清理旧 `Item-0` 隐藏记录。
- [x] 补充 `NSPrincipalClass = NSApplication`，让 SwiftPM 组装的 `.app` bundle 符合 macOS GUI app 元数据要求。
- [x] 调整 `script/build_app_bundle.sh`：优先使用本机可用代码签名身份的 SHA-1 指纹；找不到身份时回退 ad-hoc 签名。
- [x] 重新执行 `swift build --disable-sandbox` 和 `script/build_app_bundle.sh`，均通过；当前沙箱无法访问钥匙串，产物仍回退 ad-hoc 签名。
- [x] 已在真实 Finder/桌面启动路径用临时验证包复验：原生 `NSStatusItem` 仍会被 ControlCenter 移入 blocked list，签名不是主因。

## 菜单栏入口最终兜底方案

- [x] 移除 SwiftUI `MenuBarExtra` 入口；该方案在 ControlCenter 屏蔽状态下会收到隐藏/退出动作，导致应用启动后立刻退出。
- [x] 在 `AppDelegate.applicationWillFinishLaunching` 和 `applicationDidFinishLaunching` 都初始化顶部入口，避免启动阶段未进入 `didFinish` 时入口缺失。
- [x] 将入口改为自绘 `NSPanel` + `MenuBarOverlayView`，放置在菜单栏窗口层级，显示图标和 `OB` 文本，不再依赖 ControlCenter 接纳 `NSStatusItem`。
- [x] 点击自绘入口后复用原菜单项：`网关切换...`、`设置...`、`清除 OneBoard 授权...`、`退出 OneBoard`。
- [x] `script/build_app_bundle.sh` 增加 `Contents/PkgInfo`，补齐 macOS app bundle 兼容元数据。

## 最终验证

- [x] `swift build --disable-sandbox` 编译通过。
- [x] `script/build_app_bundle.sh` release 打包通过，`codesign --verify --deep --strict` 通过。
- [x] 通过 Finder 启动 `/private/tmp/OneBoardOverlayTest3.app` 验证包，诊断日志确认入口窗口创建在顶部：`frame: {{846, 899}, {64, 24}}`。
- [x] Computer Use 直接抓取 `com.oneboard.app.overlaytest3`，已看到顶部小入口截图：图标 + `OB`。
- [x] 点击 `OB` 后菜单展开，包含 `设置...`；点击 `设置...` 后成功打开 `OneBoard 设置` 窗口。
- [ ] 当前平台提升权限命令被用量限制拦截，暂未能自动覆盖 `/Applications/OneBoard.app`；正式修复包已生成在 `build/OneBoard.app`。
