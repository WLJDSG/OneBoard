# 开发日志 - 2026-06-15

## 今日完成

- [x] **重新打包**：基于当前本地改动执行 `script/package_app.sh`，重新生成 `build/OneBoard.dmg`。

## 验证

- [x] `script/package_app.sh` 通过，生成 `build/OneBoard.dmg`
- [x] `hdiutil verify build/OneBoard.dmg` 通过，checksum 有效

## 备注

- 首次在沙箱内执行打包时，`hdiutil create` 报“设备未配置”；提权重跑后成功生成 DMG。

## 系统能力联动与粘贴稳定性

- [x] 新增 `SystemCapabilityViewModel`，统一读取辅助功能、屏幕录制、网关 Helper 和开机自启真实状态。
- [x] 授权页开关改为真实状态驱动，关闭辅助功能/屏幕录制时弹确认框并自动撤销对应 TCC 授权。
- [x] 清除 OneBoard 授权后刷新设置页系统能力状态。
- [x] 修复 LaunchAtLogin helper 未打进 `.app` 导致开机自启开关弹回的问题。
- [x] 网关切换弹窗增加右上角关闭按钮。
- [x] 优化剪贴板点击条目后的目标 App 激活与自动粘贴时序。

## 本轮验证

- [x] `HOME=/private/tmp/oneboard-home CLANG_MODULE_CACHE_PATH=/private/tmp/oneboard-module-cache swift test --disable-sandbox`
- [x] `HOME=/private/tmp/oneboard-home CLANG_MODULE_CACHE_PATH=/private/tmp/oneboard-module-cache swift build --disable-sandbox`
- [x] `script/package_app.sh` 已成功构建 `build/OneBoard.app`
- [x] `build/OneBoard.app/Contents/Library/LoginItems/LaunchAtLoginHelper.app` 存在
- [x] Login helper Bundle ID 为 `com.oneboard.app-LaunchAtLoginHelper`
- [x] 本轮 `build/OneBoard.dmg` 已通过提权重跑 `script/package_app.sh` 生成，并通过 `hdiutil verify build/OneBoard.dmg` 校验。
