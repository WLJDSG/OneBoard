# 2026-09-05

## 模型目录选择与额度说明

- 根因：AIProviderQuotaPresentation 对非官方 Codex 配置直接返回固定提示，刷新动作仅调用 Codex 账号状态刷新，未接入第三方余额查询。
- 六个 Claude 模型槽位改为原生可编辑下拉框，复用当前 API Key 获取的目录；保留手动输入和 1M 标记。
- 编辑已有配置自动获取目录，连接变更清空目录，旧请求响应不再回填。
- 保留工作区已有修改，新增目录选择和手动输入回归测试。
- 验证：全量 150 项测试通过，Release 构建通过；应用打包通过，沙盒内 hdiutil 创建设备失败后在获准的沙盒外创建 DMG，hdiutil verify 校验通过。未使用真实第三方 API Key 做在线联调。

## 第三方额度、Token 用量和翻译供应商

- 参考本地 CC Switch 固定版本 c58a25b 的 balance.rs、usage_stats.rs 和代理响应记录；Sub2API 根据上游 gateway_handler.go 的 /v1/usage 契约实现。
- 新增内置额度查询、SQLite Token 事件表及按 Key 的当日/累计/缓存统计；代理分页从 0 开始，已用本地普通响应和 SSE 端到端测试确认。
- 翻译复用已添加 API Key，支持默认设置与窗口临时选择；移除旧固定 DeepSeek 客户端。
- 真实只读验证：两个 Sub2API Claude 配置及 DeepSeek 余额成功；Sub2API Codex 返回 HTTP 403 / GROUP_DELETED（API Key 所属分组已删除）。未写入或打印 API Key。
- 验证：162 项 Swift 测试、3 项 Rust 测试通过；本地假上游非流式与 SSE 代理端到端测试通过。Release 构建和 Release 代理非流式/SSE 端到端验证通过，DMG 已生成并通过 hdiutil verify。

### 参考实现

- [CC Switch 余额接口](https://github.com/farion1231/cc-switch/blob/c58a25b2ae583710f24ce8867e48340bda619f7d/src-tauri/src/services/balance.rs)
- [CC Switch Token 统计](https://github.com/farion1231/cc-switch/blob/c58a25b2ae583710f24ce8867e48340bda619f7d/src-tauri/src/services/usage_stats.rs)
- [Sub2API 额度及历史用量响应](https://github.com/Wei-Shaw/sub2api/blob/main/backend/internal/handler/gateway_handler.go)
- [DeepSeek Anthropic 兼容接口](https://api-docs.deepseek.com/guides/anthropic_api)

## 设置页整体视觉升级

- 参考 cockpit-tools 的设置截图与 sidebar CSS，自主以原生 SwiftUI 实现，不修改参考项目。
- 覆盖九个设置分类及模型、网关、账号编辑弹窗，分组侧栏、蓝青色、圆角卡片、统一字体与控件对齐；窗口可调整尺寸。
- AI 卡片采用额度摘要及当日/累计/缓存三列指标，统计范围与详细计数仍可展开。
- 设置颜色单独定义，不影响截图遮罩和工具栏；深浅色均生成实际渲染预览。
- 验证：检查九页、深色、最小尺寸及带数据卡片和编辑弹窗，162 项全量测试通过，Release 构建通过，DMG 打包及 hdiutil verify 校验通过。

## 功能分布、独立操作页与代码清理

- 按用户要求先提交并推送现有 42 个文件：8dd6bdc（origin/main）。
- 设置拆分为 11 类：识别/翻译、待办/文件分别独立；菜单按日常工具、连接账号、应用组织并补齐快捷入口。
- 新增 FeaturePanelDesign，统一翻译、OCR 结果、网关、暂存区、剪贴板、待办和历史页的深浅色、标题与内容布局。
- 网关状态消息可见，剪贴板保留时长使用实际偏好；文件暂存明确移除不会删除原文件。
- 清理无调用的旧文件行、样式扩展、颜色别名及文件窗口多余转发，保留仍有效的数据迁移和权限/协议兼容。
- 第一轮测试仅网关尺寸旧断言失败，更新后 164 项测试通过；追加可选渲染验收覆盖深浅色、最小尺寸和内容状态。最终构建与打包结果见后续记录。

- 最终验证：164 项自动测试通过，1 项可选渲染测试默认跳过；可选渲染测试单独运行通过，检查深浅色、480 高度翻译面板、长错误消息和 960×680 设置页。Release 构建成功，DMG 打包成功并通过 hdiutil verify（VALID）。真实菜单点击、文件拖放、系统授权尚未人工验收。

## 截图窗口预选、翻译和文件拖拽修复

- 文件误触发已用独立系统 NSPasteboard 复现：启动遗留文件和松手后的旧文件都能被错误确认。记录空闲/按下/松手时的 changeCount 后，同一拖拽连续帧、重新拖同一文件通过，旧文件被拒绝。统一鼠标源坐标，避免混合 Quartz/AppKit 坐标影响摇晃检测。
- 新增遮罩前窗口快照、前后顺序命中、悬停边框、单击采用窗口与直接拖动自定义；跨屏裁到本屏可见范围，保留选区移动/缩放。
- 完整工具栏增加带文字的翻译按钮；所有输出在关闭遮罩后恢复截图调用者，键盘仅处理当前遮罩。
- 用户确认贴图“刚置顶就变模糊”。原导出、SwiftUI 2x 离屏渲染及实际窗口测试未复现现场问题。显示改为直接使用最高像素位图，禁用平滑插值并显式设置初始 hosting 尺寸；未宣称现场清晰度问题已验证解决。
- 按要求下载 MacShot 与 Snapzy 到 workspace/Github-Project，记录提交与参考文件，自主实现，无第三方代码复制。
- 最终全量测试：172 项，1 项可选视觉测试跳过，0 失败。新测试覆盖 Retina 单像素条纹、实际贴图尺寸、旧文件残留、窗口预选/拖动/跨屏坐标与翻译回调。真实多屏交互、现场贴图清晰度与在线翻译仍待人工确认。
- 交付验证：Release 构建成功；正式 DMG 打包成功，`hdiutil verify build/OneBoard.dmg` 返回 VALID。未安装或替换运行中的应用，未提交本次及既有工作区改动。

### 窗口预选稳定性修正（2026-09-05）

用户反馈窗口预选仍异常后，对照 MacShot 与 Snapzy，发现 OneBoard 预选区完全透明、按下就移除高亮。两项回归在修复前出现 5 个断言失败：alpha=0 和窗口预选按下/轻微拖动后丢失。改为不透明冻结截图背景与 even-odd 外部压暗；候选窗口保留至明确拖动或松手，并补 mouseEntered 更新。相关 26 项测试通过；背景方向绘制与最终构建结果见后续记录。
- 最终 174 项全量测试，1 项可选测试跳过、0 失败；Release 构建通过。实际 NSView 绘制图检查通过：预选区不透明、原图红色定位块方向正确、蓝色边框与外部压暗正确。设备 RGB 受显示器色彩转换影响，颜色断言使用区域色系判定而非固定设备值。系统鼠标穿透风险由不透明窗口与背景消除，但未完成已安装应用的真实鼠标交互验收（CUA 获取应用超时）。
- 窗口预选修正版 DMG 打包成功，hdiutil verify 返回 VALID。产物：build/OneBoard.dmg；未替换已安装应用。

### 翻译、DeepSeek、剪贴板、Helper 与 iCloud 桌面修复

- 回归复现：DeepSeek `/anthropic` 连接错误生成 `/anthropic/user/balance`；截图工具栏事件冒泡会清掉选区；缺 Helper 时跳过安装直接请求指纹。已分别修复并新增回归覆盖。
- 选中文字未授权先引导，空白选区静默，优先 AX 读取。
- 网关安装/授权检查先于身份验证，取消安装不切换；保留网关面板，授权后重新读取网络状态。
- 长文本预览 100 次基准由约 2.05 秒降低到约 0.001 秒（仅预览处理基准，不代表整体滚动帧率）；图片后台生成 56px 缩略图、限量缓存。
- iCloud 桌面增加菜单栏与文件设置的新建入口，沿用主应用文件创建器，支持三种类型；同步根容器监听与权限声明。未修改系统 iCloud 配置。
- 代理普通/SSE 使用本地假上游验证 Token 与缓存；余额错误独立展示。真实 Key 的线上历史 Token 不作推算。

- 最终验证：187 项测试，186 通过、1 项可选视觉导出跳过；Release 构建通过；Release 代理在 Anthropic / OpenAI Chat 两种格式下普通响应与 SSE Token 计数通过；正式 DMG 通过 hdiutil verify（CRC32 86CBD0A7）。真实授权、指纹焦点、iCloud 桌面写入及整体滚动手感待安装后验收。代码未提交。

### 2026-09-05 设置首次打开崩溃修复

设置闪退修复：23:07:08 / 23:07:10 崩溃日志显示 dispatch_once 递归锁。上次加入 sudo 授权探测后，SystemCapabilityViewModel.shared 初始化的 refresh 在主线程等待子进程，嵌套 RunLoop 触发布局再访问 shared。将全部该 ViewModel Helper 检查移至后台；新增线程断言回归（修复前失败）及独立进程实际设置窗口首次打开测试。

- 回归结果：188 项测试，187 通过、1 项可选视觉导出跳过。独立 App Bundle 冷启动设置、布局、再次打开均通过。普通 XCTest 宿主缺少通知服务所需 Bundle，实际窗口验证改为临时独立 App 包，未以宿主限制修改生产权限逻辑。
