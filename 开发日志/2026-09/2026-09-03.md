# 2026-09-03 Codex OAuth 账号授权

## Finder iCloud 桌面右键新建调查结论

- 现场复现为用户主目录右键可显示 OneBoard“新建文件”，iCloud 桌面空白处不显示；Finder 的无障碍路径把该桌面容器暴露为 `~/Library/Mobile Documents/com~apple~CloudDocs/`。
- 实机分别安装“只注册真实 `~/Desktop`”和“只注册 iCloud Drive 根目录”的诊断扩展；Finder 均未调用目录监听或菜单回调，排除路径重叠和沙盒 entitlement 缺失。
- 扩展沙盒中的 `FileManager.desktopDirectory` 实际解析到容器内 `Data/Desktop`，因此桌面容器缺少目标 URL 时必须回退到通过系统用户信息得到的真实 `~/Desktop`。
- 结合 Apple DTS 对 Finder Sync 目录限制以及 iCloud Drive 无法监听的公开说明，确认 File Provider 管理的 iCloud 桌面无法由第三方 Finder Sync 添加空白处菜单。
- 撤销无效的 iCloud Drive 根目录监听与只读例外，清理诊断日志，并在设置页、授权提示和项目文档中明确平台限制；本地 Finder 目录的新建文件链路保持不变。

## 目标

- 用 cockpit-tools 同类的浏览器 OAuth 流程取代“保存当前登录”作为新增账号主入口。
- 用户填写邮箱、打开 Codex 官方授权页，浏览器授权成功后自动添加或更新账号。

## 实现

- 新增 `SystemCodexOAuthAuthorizer`：随机 PKCE verifier/challenge、随机 state、官方登记的 `localhost:1455` 回调、10 分钟超时和 OAuth token exchange。
- 授权链接直接使用 Codex CLI 标准形态请求 `https://auth.openai.com/oauth/authorize`，不经过 Codex Desktop 私有中转页。
- 从 `id_token` 读取已授权邮箱与套餐标识，从 access token 读取 ChatGPT account ID；填写邮箱与实际授权账号不一致时不保存。
- 认证缓存使用 Codex `type/tokens/last_refresh` 结构，统一保存在 OneBoard SQLite vault。
- 新增 OAuth 账号弹窗，保持 OneBoard grouped Form、颜色和系统控件风格。

## 授权与翻译故障修复

- 真实授权返回 `invalid_authorize_request` 的原因是 OneBoard 套用了 Codex Desktop 私有中转页和 Desktop 专用参数，页面又注入当前工作区信息，最终形成不兼容授权请求。
- OAuth 改为 `originator=codex_cli_rs` 的直接授权请求，移除 `codex_app_version`、`source_surface_stable_id` 和 `codex_origin_stable_id`。
- 实测随机 localhost 端口会被授权服务拒绝；回调固定为官方 Codex CLI 使用的 `http://localhost:1455/auth/callback`。
- Google 翻译失败可稳定复现为免费 Web 端点返回 HTTP 429 和自动查询拦截 HTML；现在识别限流并提示稍后重试或切换 Apple/DeepSeek，不再展示整页 HTML。
- 新增 OAuth URL 结构和 Google 429/HTML 回归测试。

## 安全边界

- OneBoard 不收集、代填或保存 OpenAI 密码、MFA 或邮箱验证码。
- OAuth 回调只监听本机 loopback，并且必须通过 state 校验才接受 authorization code。
- 切换仍必须先退出 Codex 并确认旧进程完全结束。

## 验证

- OAuth 本机回调与模拟 token endpoint 集成测试通过。
- 账号新增、同邮箱更新和邮箱不匹配拦截测试通过。
- `swift test --disable-sandbox`：111 项测试通过，0 失败。
- `swift build -c release --disable-sandbox`：Release 构建成功。
- `hdiutil verify build/OneBoard.dmg`：镜像校验有效；SHA-256 为 `29f7dcf59e4921881836a6090079757b990d4a3dec9aafc4bf3ff2f979a84d73`。
- 浏览器对照验证：随机回调端口转入 `unknown_error`，`localhost:1455` 可正常进入 OpenAI 登录页；未输入账号，未提交授权。
- 真实 OpenAI 账号的浏览器授权需要用户在官方页面完成，未纳入自动化验收；自动化已覆盖授权 URL、state 回调与 token exchange。

## Codex 账号额度与凭据自动续期

### 实现

- 参考 `cockpit-tools` 的当前实现读取 `wham/usage`，将服务端 `used_percent` 转换成设置页需要的剩余百分比，并展示 5 小时/每周窗口及重置时间。
- 从 usage 响应读取套餐和剩余主动重置次数；usage 未返回次数时单独查询重置凭证接口；从账号检查与订阅响应补充订阅到期时间，字段不可用时明确显示未知。
- App 启动后每 15 分钟顺序刷新所有账号，也支持设置页手动刷新；失败账号保留上次成功快照，不阻断其他账号。
- access token 提前 5 分钟续期；额度接口返回 401/403 时允许一次续期后重试，并保存服务端轮换后的完整认证缓存。
- 切换账号前先续期目标账号。当前账号正在 Codex 中运行时不由 OneBoard 旋转 refresh token，避免两个客户端并发使用造成凭据失效。

### 安全边界

- access token、id token、refresh token、额度和订阅快照统一保存在 OneBoard SQLite；不再使用 macOS Keychain。
- 自动续期不读取密码、MFA 或邮箱验证码，也不消费主动重置次数。
- 当前额度与订阅查询接口并非 OpenAI 公开稳定 API；实现按字段缺失和接口失败做降级，不伪造到期时间或额度。

### 验证

- `swift test --disable-sandbox`：119 项测试通过，0 失败。
- `swift build -c release --disable-sandbox`：Release 构建成功。
- `ONEBOARD_CODESIGN_IDENTITY=- script/package_app.sh`：正式 DMG 重新生成成功。
- `hdiutil verify build/OneBoard.dmg`：镜像校验有效；SHA-256 为 `c74554a612aa65dec6f4b08d35ab62d88d293f129e00bcffd0b82befedf98ac6`。
- 未使用真实账号自动化调用远端额度接口，避免读取或输出用户凭据；真实账号的额度/订阅展示仍需安装后验收。

## Codex / Claude Code 底层模型切换

### 实现

- 基于 cc-switch 当前源码的 live config 投影边界，新增 `AIModels` 模块。
- 支持 Codex 和 Claude Code 的官方/自定义 API 配置 CRUD、底层模型 ID 以及菜单栏快速切换。
- 供应商元数据、当前标记和 API Key 统一保存在 OneBoard SQLite，彻底卸载同步清理 Application Support 目录。
- Codex 管理 `~/.codex/config.toml` 的顶层模型和 `[model_providers.oneboard]`；Claude Code 管理 `~/.claude/settings.json.env` 的 API/模型键。
- 首次切换前生成 `.oneboard-backup`，使用原子替换和 `0600` 权限，支持设置页一键恢复。

### 安全边界

- Codex 模型切换不读写账号认证缓存，与桌面账号切换完全隔离。
- 第三方 Codex API Key 常态保存在 SQLite；激活时受 Codex 格式约束，作为 provider-scoped `experimental_bearer_token` 写入 `0600` 的活动 TOML。
- 不主动退出或重启 Codex / Claude Code，只对新会话承诺生效。

### 验证

- AI 模型聚焦测试：7 项通过，覆盖 Codex TOML 保留、Claude JSON 合并、备份恢复、符号链接拦截和双工具独立活动状态。
- 真实 API Key 和远端请求未用于自动化验证；安装后需用测试供应商新建会话确认实际路由。

## OneBoard 私有数据统一迁移到 SQLite

### 实现

- 新增 `private_records` 与 `application_state` 表，Codex 账号认证缓存、OAuth token、AI Provider API Key、OCR/翻译 API Key 及相关元数据统一存入 `~/Library/Application Support/OneBoard/oneboard.sqlite`。
- 数据库目录强制 `0700`，主库与已存在的 WAL/SHM 文件强制 `0600`；不提供 macOS Keychain 读写实现。
- Codex 当前运行凭据仅按客户端要求物化到 `~/.codex/auth.json`，并把 `cli_auth_credentials_store` 强制改为 `file`，首次修改保留稳定备份。
- 新增 CC Switch 只读导入器和设置页入口，支持 Codex TOML、Claude env、分层 Haiku/Sonnet/Opus 模型与当前配置标记；缺必要字段的配置显式跳过。

### 本机迁移

- 将 2 个 OneBoard Codex 账号元数据及 2 份认证缓存写入 SQLite，逐字节校验成功后删除原 `com.oneboard.mac.codex-auth-cache` 钥匙串条目。
- 从 `~/.cc-switch/cc-switch.db` 导入 3 个 Codex、3 个 Claude 配置和 5 个 API Key；空白 `Claude Official` 因无模型 ID 被跳过，源数据库未修改。

### 验证

- SQLite 回归覆盖凭据读写、旧偏好迁移、CC Switch 端到端导入和当前标记。
- 生产 Swift 源码不存在任何 `SecItem*` 调用；Security.framework 仅保留给 OAuth PKCE 安全随机数。
- `swift test --disable-sandbox`：127 项测试通过，0 失败。
- `swift build -c release --disable-sandbox`：Release 构建成功。
- `ONEBOARD_CODESIGN_IDENTITY=- script/package_app.sh`：DMG 打包成功；镜像内应用通过 `codesign --verify --deep --strict`。
- `hdiutil verify build/OneBoard.dmg`：镜像有效；SHA-256 为 `f6002626bd80b05ba9a6d20b500d7682b25c75579cd5f78000c13e89ee15c867`。

## Claude Code 角色模型映射与模型页额度

### 实现

- 对照 CC Switch 当前 `ClaudeModelEnvField`，在 OneBoard 配置模型中补齐 Haiku/Sonnet/Opus/Fable 的模型显示名、Fable 模型和 `CLAUDE_CODE_SUBAGENT_MODEL`。
- 编辑弹窗新增可折叠槽位配置，并解释 `[1M]` 后缀；切换时 Haiku/Sonnet/Opus 空值回退默认模型，Fable 空值先回退 Opus。
- CC Switch 只读导入同步接收上述角色模型、显示名和子代理模型。
- AI 模型配置行显示额度摘要并可刷新：官方 Codex 复用当前账号的 5 小时/每周快照；本机第三方配置没有用量查询元数据，因此明确显示“未配置查询”。

### 安全边界

- CC Switch 支持可执行 JavaScript 用量脚本，但当前本机供应商未配置该能力；OneBoard 不静默导入或执行任意脚本，也不以本地请求计数冒充服务端余额。

### 验证

- `swift test --disable-sandbox`：130 项测试通过，0 失败；新增覆盖完整 Claude 槽位写入、Fable 回退、CC Switch 导入和额度降级展示。
- `swift build -c release --disable-sandbox`：Release 构建成功。
- `ONEBOARD_CODESIGN_IDENTITY=- script/package_app.sh`：DMG 打包成功；`hdiutil verify build/OneBoard.dmg` 验证有效。
- 新产物 SHA-256：`cf90dc79583327d2d6677b3a1f544dcf5eae88fd1d96495b2f5840dd390bbea9`。
