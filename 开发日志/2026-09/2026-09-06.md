### 22:31 长截图、标注字号与刘海暂存回归修复

- 将长截图完整矩形边框改为四角短标记，消除长选区左右两根贯穿线；保留 ScreenCaptureKit 自身窗口排除与鼠标透传。
- 将非游标标注工具的颜色、粗细/字号步进控件恢复为工具栏内联，避免非激活截图面板中的 popover 无法稳定点击。
- FileDropTarget 覆盖完整投放卡片并兼容 pasteboard item、单值 file URL 与 NSFilenamesPboardType；暂存入库仍只接受真实普通文件。
- AirDrop 分享期间保持服务对象和暂存面板，由系统收件人选择完成/失败回调结束会话；刘海面板以 0.30 秒展开、0.24 秒反向缩回。
- 新增四角线段、内联字号、Finder 载荷和刘海动画几何回归；快速测试 5 项通过，真实 Finder 拖放/AirDrop/动画仍需安装包人工验收。

## 设置闪退修复交付确认

### 快捷键与图标后续修订

- 实际查看浮引编辑网格、截图主工具条及样式展开；其长截图未获得可验证界面。
- 单一快捷键注册源、旧绑定迁移、类型弹出菜单、菜单栏同高单色图标、双行网速、跨日运行时长、截图取色器。
- 关闭长截图遮罩与边框的窗口阴影；两条长线是否完全消失仍需实机验收。
- 完整测试 213 项，跳过 1 项，0 失败。视觉渲染与完整测试一起运行触发 XCTest 无应用 Bundle 的通知权限异常，改为单独运行视觉测试。

### 17:14 交互回归与文件类型扩展

17:15 Release/Finder 扩展/DMG 打包成功，hdiutil verify VALID。SHA256：ada26f2c57de89e13e2e0a4e9da2e33643bfbaf015648836b9394edbbf3643e8。使用现有 Apple Development 签名，未声称完成公证或安装验收。

- 按 diagnosing-bugs 先运行失败用例：悬停立即开卡出现 3 处断言失败；自定义 vue 后缀被旧枚举拒绝。修复后通过。
- 700ms 连续停留、日历对齐/相邻月份导航、状态卡片完整布局/彩色进度/应用图标/悬停与系统入口、菜单栏指标配置、剪贴板即时单击预览、刘海顶边定位与原生 URL 拖放、截图浮动窗口候选/集中调色、快捷启动网格及备份字段。
- 长截图批量灰度转换、限制高分辨率匹配行数、80ms 采样等待；稀疏文本与多位移回归通过。不承诺没有重叠时能恢复丢失内容，也未量化真实快速滚动提升。
- Finder 支持 15 种内置后缀及自定义安全后缀，主应用创建、扩展仅转交 URL，移除终端后额外分隔。专有文档不会伪造模板。
- 最终全量 210 项、1 可选跳过、0 失败；视觉专项单独通过。图片位于 /private/tmp/oneboard-review-1712，填充三条网络应用数据，已检查状态全卡与日历基线。
- 本轮未替换运行中的 /Applications/OneBoard.app；Finder 拖放、AirDrop 收件人界面、实际双击粘贴及快速滚动需安装验收。静态渲染不代表系统交互已通过。
- 快捷指令遵循 Apple 官方 URL scheme：https://support.apple.com/guide/shortcuts-mac/run-a-shortcut-from-a-url-apd624386f42/mac 。

### 16:28 菜单栏、刘海暂存与状态卡片交付

- 读取用户两段录屏关键帧（88.725 秒、42.558 秒），确认长截图暗色遮罩/右侧拼接预览/底部控制条及顶部中央暂存/AirDrop 交互。录屏文本不视为指令。
- 开源 UI 参考经 awesome-agent-skills 查找到 AvdLee/SwiftUI-Agent-Skill，阅读 macOS 窗口和布局指南，采用原生无标题栏卡片、统一状态管理，不额外安装全局技能。
- 日历真实面板测试先复现三条失败：titled/closable 未移除、第二次 show 未隐藏。改为 HoverCardController 后通过，增加图标→面板穿行、移出、置顶和主动关闭 hover 抑制测试。
- 长截图白底稀疏文字滚动23像素先返回nil；以跨行方差替代横向纹理判断后匹配正确。首帧与后续改用同一 ScreenCaptureKit 路径；加入选区外遮罩和右侧实时预览、后台匹配。
- 剪贴板单击预览，双击复用原活动应用复制粘贴路径。
- 顶部中央刘海区支持双投放区（暂存/AirDrop）、暂存文件拖出与移除，系统分享面板由用户确认收件人。
- Mac 状态新增菜单栏卡片：host CPU/VM、物理接口流量、IOPS电池、热状态、磁盘与运行时间；nettop 进程差值从首次采样累计。真实系统容量及采样历史测试通过。
- 全量208项：207通过、1可选视觉跳过；视觉专项独立通过，Release构建与DMG校验通过，git diff --check通过。
- DMG SHA-256：`f3c44016f23bee048cc7780d18fc9e0770cba41d08e4db64ac87e130d512e003`。
- CUA连接新版app路径超时，同Bundle ID存在多个副本导致标识歧义；未将自动测试或静态渲染宣称为真实连续滚动、双击粘贴、AirDrop发送验收。未替换/Applications应用，未发送用户文件。

### 15:47 六项反馈修订

- 根据崩溃报告确认 QLPreviewView.deactivate 断言，改为异步文件缩略图与 SwiftUI 快速查看，删除剪贴板右上角关闭按钮；20 次文件预览/文本销毁切换回归通过。
- 日历固定六行并让标题栏避开月份导航，加入 LunarSwift 的 24 节气、中国节日与截至 2026 年公布的休假/补班数据。未公布年份不推算调休。今天圆形、节日/节气标签、右侧分类均已渲染检查。
- 参考 Snapzy 的自身窗口过滤方式，长截图改用 ScreenCaptureKit 排除本应用窗口，不再每帧隐藏边框；手动滚动及匹配边界不变。
- 按最新需求改为 iCloud Drive/app/oneboard 明文配置备份，保留上一版，首次恢复下载失败时保留恢复意图；包含快捷键和已有可迁移配置，排除历史内容数据。未对用户真实云盘执行恢复测试。
- 翻译选择已有 AI 供应商，并复用共享编辑器新增，不切换活动客户端。菜单栏改为 18pt 单色叠层面板模板图标。
- 最终全量 204 项：203 通过、1 可选视觉导出跳过；视觉专项独立执行通过。混合视觉与全量曾触发测试宿主通知权限异常，分开运行后通过。Release 与 DMG 校验通过。
- DMG SHA-256：`7bb6a0707cac51133d488e45cd4f73b9c5d70fd66e60ad49c93301964df25ec2`。真实屏幕手动滚动/iCloud 服务端上传仍待安装验收。
- 镜像内应用通过 open 启动并持续运行超过 11 秒，codesign --verify --deep --strict 通过；测试后关闭该镜像进程并卸载镜像，未替换 /Applications 原应用。

### 日历、长截图、同步与剪贴板交互修订

- 日历双栏、农历、自定义倒数日与待办、置顶；剪贴板类型筛选与右侧文本/图片/Quick Look 预览。
- 长截图去除辅助功能门禁与自动滚动，改为用户手动滚动、图像重叠匹配和完成/取消。
- 本地安装包支持用户选择 iCloud Drive 同步目录，AES-GCM 加密、SQLite 本地密钥、文件协调与下载占位处理；不再以 CloudKit 签名能力禁用开关。
- 全量 199 项测试：198 通过、1 可选视觉跳过；随后视觉渲染专项通过。Release 构建通过，原生启动进程驻留。CUA 连接应用超时，真实滚动与双机同步仍待人工验收。
- 最终 DMG 校验 VALID，SHA-256 `7612af0c159c6364c9f94aea133fae3b618ea0a1976df051cf0ef88b5e7e6a61`。

延续 9 月 5 日设置初始化重入修复：Debug 与 Release 独立 App Bundle 冷启动设置、布局和再次打开均通过；全量 188 项测试中 187 通过、1 项可选视觉导出跳过。正式 build/OneBoard.dmg 已重新生成，hdiutil verify 通过（CRC32 6DD8A159）。未替换 /Applications 中的应用，代码未提交。

## iCloud 同步、日历、Finder 终端、长截图与翻译设置

- 增加 CloudKit 私有配置快照、同步状态页、偏好/SQLite 变更监听与远端删除传播；同步敏感配置但排除设备权限、运行状态、额度及用量。
- 增加月历模型、设置预览、周起始日与独立菜单栏图标开关。
- Finder Sync 增加当前路径终端请求，继续由主应用执行实际动作。
- 截图框选增加长截图动作：关闭遮罩后自动滚动、重抓、静止检测、拼接并进入标注窗口。
- 翻译设置改为系统、Google、自定义 API 三类，自定义支持预设、地址、Key、模型目录和连接测试。
- 新增 `CloudSyncAndCalendarTests` 及 Finder 终端请求回归；首轮新增测试 7 项通过。全量测试的既有 DragDetector 系统剪贴板用例在当前受限会话仍失败，待最终复跑记录。
- 最终相关回归 67 项全部通过；全量 195 项中 191 通过、1 项视觉导出跳过，只有既有 DragDetector 用例的 3 个系统剪贴板断言失败。
- `swift build -c release --disable-sandbox`、正式打包和 `hdiutil verify build/OneBoard.dmg` 通过；DMG SHA-256 为 `3145da4a5d4fee121baa6dc6b533f48fa6072b97281c474105f4738bf9e0314a`。
- 受限环境无法挂载 DMG（`hdiutil attach` 返回“设备未配置”）；CloudKit 双机同步、Finder 右键、独立菜单栏图标和真实滚动长截图仍需安装包人工验收。

## 安装后无法启动修复

- 复现安装包可执行文件在 0.1 秒内退出 137；`spctl` 报 Code Signing subsystem internal error，确认临时签名携带 CloudKit 受限 entitlement 被系统装载阶段拒绝。
- 修复构建脚本错误：Swift 构建的临时 `HOME` 不再污染后续 codesign，因此本机 Apple Development 身份可正常访问钥匙串。
- 本地 DMG 改用不含 CloudKit 权限的 `OneBoard.local.entitlements`；正式 iCloud 构建需有效 provisioning profile 并显式设置 `ONEBOARD_ENABLE_ICLOUD_SYNC=1`。
- `CloudSyncViewModel` 延迟创建 `CKContainer`，无 iCloud 能力的包禁用开关并显示说明，避免启动阶段 SIGTRAP。
- 7 项相关测试和 Release build 通过；重新打包后 `hdiutil verify` 通过，SHA-256 为 `bcba69a61fba667477c360a66a440f1ce9ffb4d9a9cb324e65dade18d48e64f8`。
- 已挂载新 DMG 并通过 macOS `open` 启动镜像内应用，进程持续驻留；完成后结束测试进程并卸载镜像。
# 交互、菜单栏与截图样式修订

- 文字识别联网模式改为选择 AI 模型页已有服务，复用其连接、视觉模型和 SQLite API Key。
- 快捷键页移除键位网格下方的重复列表；绑定应用与 OneBoard 工具改用独立搜索选择器。
- 文件拖拽在确认本次 `.drag` 文件数据后立即展开暂存区，同一拖拽只触发一次，不再要求摇晃。
- Mac 状态与日历菜单栏入口均可在设置中关闭；网速采用上传/下载上下两行。
- 日历选中日期使用强调色填充和描边；网关配置改为可预览的系统图标选项。
- 截图工具栏统一为紧凑玻璃卡片并将长截图收入输出组；长截图控制卡和实时预览采用一致的圆角、描边与阴影。
### 22:53 全量失败与设置交互收敛

- 修复原有 9 个断言失败：本地备份读写、拖拽 changeCount、磁盘容量回退与日历悬停坐标。
- Codex Pro 额度按窗口时长识别；AI 供应商和 Codex 账号支持拖拽排序。
- 模型目录可直接选 Codex 默认模型，Claude 一键设置改为先选模型。
- 快捷键目标选中即绑定，网页支持图片或域名缩略图；网关与 Mac 状态移除图标选项；设置页顶部留白收紧。
- 全量测试 219 项，1 项跳过，0 失败。

## 供应商、权限和截图体验修订

完成供应商模板/独立模型目录与额度接口、拖拽占位、直接绑定、退出生命周期与等待、目录授权书签、安静备份、文字拖动/字号、框选标注、实时 RGB 取色、弹窗候选和长截图匹配/外观修复。重复页面静止但徽标变化的测试复现旧算法错误追加 24px，修正后拒绝追加。具体实现、设计技能参考和验收边界见 [修复记录](../../docs/修复记录/2026-09-06-供应商与截图体验.md)。

最终验证：`swift test --disable-sandbox`（开启本轮界面渲染）共 236 项、1 项跳过、0 失败；Release 构建成功；`codesign --verify --deep --strict` 通过；DMG `hdiutil verify` 返回 VALID。沙盒 hdiutil 初次报“设备未配置”，通过沙盒外同一映像创建操作完成。产物 `build/OneBoard.dmg`，SHA-256 `e611f076dc36ad32f04383e9846144195a346c25e1e2c19213c4c54452e653c1`。未提交或推送代码。
