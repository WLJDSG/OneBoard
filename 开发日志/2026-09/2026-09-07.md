# 2026-09-07

## 截图、官方账号与授权交互

完成用户 15 项有效反馈对应的代码修改；第 16 项为空。详情见 [修复记录](../../docs/修复记录/2026-09-07-截图账号与授权.md)。

- 编号断号回归先失败，再修复连续序位、点击编辑、删除补号与操作快照撤销。
- 悬停测试在旧 mouseExited 实现下得到 nil；刷新实际指针位置后通过。保留所有正常候选窗口，未按应用 PID 排除 OneBoard 自身可见窗口。
- 长截图恢复外部暗罩；保留四角、透传与采集排除自身。
- 主菜单标准编辑命令、Key 区复制粘贴按钮、官方模型缓存目录、原生排序载荷、官方账号授权入口、备份文件夹打开与授权页入口更新。
- 工具栏深浅色离屏图已检查，T 字样与两行五色色板、内联样式值、原图像素块马赛克更新。

## 验证

- 最终 `ONEBOARD_EXPERIENCE_RENDER=/tmp/oneboard-september-render swift test --disable-sandbox`：247 项，245 通过，2 跳过，0 失败。
- 两项跳过：未启用的 FeaturePanel 按需渲染测试；ScopedBookmarksAgent 不允许未签名 xctest 创建安全作用域书签，需签名应用内验证。
- `swift build -c release --disable-sandbox` 通过；打包脚本对最终代码再次执行 Release 构建，通过。
- `git diff --check` 通过。
- 应用与 DMG 源目录的 `codesign --verify --deep --strict` 通过。
- 签名为本机 Apple Development 证书，SHA-1 `525E49180EC9AE646202F2444C14D65C2E75E4A1`，TeamIdentifier `PM67CWPMYW`，非 ad-hoc。
- `hdiutil verify build/OneBoard.dmg` 通过，最终输出 `checksum ... is VALID`。
- 打包脚本的 hdiutil create 在沙箱内报“设备未配置”；申请系统磁盘映像操作后，以同一已签名 dmg-root 成功生成 `build/OneBoard.dmg`。

## 实机边界

Computer Use 连续连接 `/Applications/OneBoard.app` 超时；不能宣称原生拖拽、Key 快捷键、微信场景、真实 Codex/Claude OAuth、iCloud Finder 打开或重启权限已实机验收。现有安装包经检查为 ad-hoc，此轮使用开发证书打包；用户未明确第 6 项具体弹窗，签名不能解释所有系统权限问题。首次更换签名可能仍需一次系统授权。

没有操作用户真实账号登录，没有切换当前 Codex 账号，没有安装或替换 `/Applications/OneBoard.app`。


## 2026-09-07 iCloud 授权与 Claude Code 账号

- 文件夹授权按规范化文件系统路径校验，忽略目录 URL 末尾斜杠；授权错误显示在对应行。选择后立即重试已开启的备份，异步读写期间保持安全作用域访问。
- 授权状态与操作统一样式，文件夹使用已连接状态、更改和断开操作。
- 新增独立 Claude Code 账号页，复用 AI 模型配置与 SQLite 授权，支持添加、切换、编辑、重新授权和删除；切换后新会话生效。
- 内置代理检测父进程存活；OneBoard 强退或崩溃后代理自行停止并释放端口，正常退出继续使用既有清理路径。

验证：Swift 全量 250 项，0 失败、4 项跳过（按需渲染及未签名书签环境）；设置页独立进程渲染通过；Rust 4 项单元测试通过，真实代理父进程 SIGKILL 后释放端口的集成回归通过。Release 构建及签名校验通过。系统授权弹窗与真实 Claude OAuth 尚未实机验收，Computer Use 访问已安装应用超时。

Claude Code 账号页不展示共享的供应商切换状态，避免串入 Codex 提示；仅显示本页操作错误。

Claude Code 授权页与 Codex 统一标题、待授权账号、OAuth 卡片、浏览器等待状态、链接复制及取消操作；按 Claude 授权流程保留授权码回填，账号输入位于弹窗内。

## 暂存拖放与截图文字、马赛克样式

- 复现：新增原生命中测试，两个暂存投放区实际命中外层 NSHostingView，而非 Destination，修改前 2 项断言失败。
- 修复：FileShelfHostingView 显式转交原生拖放区域；文件拖出使用 NSURL + NSDraggingItem。新增拖出系统剪贴板和命中回归；暂存失败显示具体错误。
- 动效：窗口透明度固定为 1，内容跟随窗口从顶部中央缩放；展开使用快速起步、平滑减速曲线，反向收起。收起中重新打开不再保留旧折叠窗口。
- 文字：取消输入材质底、整圈边框和八向输入手柄；保留底部定位线、移动柄、右下缩放点，以及既有 Enter 提交、Esc 取消和双击编辑。完成后不再显示悬停边框。
- 马赛克：按原图固定方格采样，拖动边界不重新拉伸像素块；拖动预览不透明，预览与导出继续共用同一像素生成逻辑。
- 验证：全量 254 项，250 通过、4 个按需渲染测试跳过、0 失败。单独运行暂存/马赛克/渲染回归 7 项通过，并检查了两张渲染图。原生剪贴板写入需要沙盒外测试，沙盒内拒绝不代表产品错误。
- 验收边界：尚未完成新安装包在 Finder 中实际拖入再拖出及刘海动画的人工观感验收。
- 交付：Release 构建通过；使用固定 Apple Development 证书 525E49180EC9AE646202F2444C14D65C2E75E4A1 签名并完成包内签名校验，生成 build/OneBoard.dmg。样式图保存在 build/visual-review/。
- DMG 校验：`hdiutil verify build/OneBoard.dmg` 返回 VALID，CRC32 CD95D80A。

## 暂存接收、悬停和原位编辑二次回归

- 根因补充：前轮只验证 hitTest。新增 FileShelfDropSessionTests 直接走 HostingView 的 AppKit 拖放回调，修改前 draggingEntered 返回拒绝、perform 返回失败、receive 未触发，共 3 项断言失败。现在注册文件类型并转交完整拖放回调，空白区域回退为暂存。
- 刘海连续悬停 0.5 秒展开，离开面板与刘海 0.15 秒后开始收起；计时器加入 common RunLoop，拖动期间仍可计时。AirDrop 保持会话。按追加要求删除右上角收起箭头。
- 暂存视觉：黑/深灰底，主暂存区、独立 AirDrop 区、带文件大小的文件卡片及空状态。
- 工具栏色板位置固定，只有数值调节器带底框；按工具标注字号、线宽、编号、颗粒。
- 文字单击选中与首次输入使用相同底线、移动柄和右下缩放点；双击复用原位输入器，提交保留图层 ID。编辑与一次位置拖动支持撤销，取消编辑不写入原图层。
- 最终全量测试：258 项，254 通过、4 个按需渲染跳过、0 失败；额外样式渲染已单独运行通过。
- 用户明确授权本机安装及 AppleScript/CGEvent 桌面自动化。已安装固定证书版本，并在 Finder 创建 Source/Destination 临时目录实际验证：文件拖入后暂存显示 1 个文件；从卡片拖出后目标目录出现文件，cmp 与源文件一致；原文件仍在。
- 实际窗口时序：鼠标在外 false，刘海停留 250ms false，停留 1050ms true，移出 650ms false。结合注入时间回归确认 0.5 秒连续阈值与离开重置。
- 实际截图验收：输入 OneBoard 测试，Enter 提交；单击后拖动 50pt/50pt，控件跟随；双击后 AX 输入框保留原文，修改为“双击编辑成功”并提交。切换马赛克后色板保留、标签“颗粒”，加号使 6→8，并实际绘制固定方格。
- CUA 对无普通窗口的 OneBoard 读取超时；Finder 和截图窗口可读，主线程采样处于正常事件循环。补充系统鼠标自动化后完成跨窗口拖放，不能把该工具限制当作应用无响应。
- 最终交付：固定开发证书 Release 包签名通过，DMG 校验 VALID，CRC32 46A3C868；从该 DMG 安装到 /Applications/OneBoard.app 后再次确认右上角无箭头、悬停 250ms 未展开/1050ms 已展开、移出 650ms 已收起。实机局部截图保存于 build/visual-review/shelf-final.png 和 shelf-final-empty.png。
- 最终安装启动曾出现“访问其他 App 的数据”系统授权框；用户明确同意继续授权后，再次检查时弹窗已消失，未代替用户点击其他权限设置。此轮不据此宣称 TCC 升级或重启权限问题已解决。
- 已移除本次 staging-check.txt 暂存记录，并关闭 Source/Destination 临时 Finder 窗口；源文件与拖出副本保留在临时测试目录。

## 2026-09-07 文件拖动保持展开、刘海全域与权限精简

有效文件拖动期间暂存区自动展开并持续保持，松手后按鼠标位置恢复收起；不把旧 .drag 载荷或普通窗口拖动当成文件会话。刘海触发区使用系统实际刘海宽度，覆盖整个刘海及顶边，不向两侧扩张，底部仅保留 2pt 容差；普通悬停仍需连续 0.5 秒。暂存面板以顶部内凹肩线连接屏幕边缘，避免直角；展开收起仍保持不透明。

文字与框选文字字号限制为 15–25，逐次加减 1，到边界停止；加减按钮整个 22×28pt 区域都可点击。文件拖动改为无权限轮询，移除输入监控监听、检测和设置入口，以及无实际 Finder Apple Events 调用的自动化声明。保留截图、选中文字/粘贴、提醒、用户目录授权等仍有用途的权限；卸载清理仍兼容历史授权记录。

- 根因：自动收起状态没有纳入有效文件拖动；刘海热点到物理底边为止且 CGRect 不包含 maxY 顶边；字号减号使用 plain Button 时仅图形命中。实机修改前点击减号上方留白数值仍为 18，点击符号中心才变为 17。字号有界回归先运行旧实现，6 项边界断言失败。
- 最终范围按追加反馈收窄：实机系统刘海左右边界为 751/960pt，高度 38pt；顶边、左右顶角、底部下方 1pt 和左右底角均可触发，左右各外移 4pt、底部外移 6pt 均不触发。外观改为顶部内凹肩线，实机截图已检查。
- 输入监控代码、状态缓存和设置入口已删除；无 Finder Apple Events 调用，删除对应例外 entitlement 与 UsageDescription。tccutil 对 com.oneboard.mac 的 ListenEvent 和 AppleEvents 撤销均成功；辅助功能、录屏、通知和目录访问仍有真实用途，保留。
- 无输入监控下的实机文件拖入：在远离刘海的 (500,500) 持续按住，面板保持展开，再移至暂存区投放，文件成功显示。字号按钮留白点击 18→17，连续减停在15、连续加停在25；记录了局部截图。
- 最终全量回归 259 项，255 通过、4 跳过、0 失败；Release 与固定证书签名校验通过。最终 DMG 校验 VALID，CRC32 4001495C，已从该 DMG 安装。
- 本次同类“访问其他 App 数据”启动弹窗再次出现，按用户已明确授权点击允许后继续；未将此归因为普通重启或宣称权限稳定性已解决。
- 最终原生拖出复测通过：暂存卡片拖入 Finder Destination3，cmp 确认与 Source 原文件一致；已清理本次暂存记录及测试 Finder 窗口。长按拖出检查在两个远离刘海的位置仍保持展开，松手后收起。

## 刘海顶边展开闪缝

- 实机录屏逐帧复现：第28帧顶部0/1行仅鼠标黑点，而第2行已出现598像素面板；第29帧顶部仍露底，第1行已有707像素面板。观察到1–2像素背景缝，定位到原生窗口尺寸动画与内容布局之间的边界偏差。
- 原生窗口从第一帧即固定为展开范围，内容使用顶部中央缩放；收起后关闭窗口，保留动画代次保护。新增原生窗口首帧/收起时frame不变且alpha为1的回归。
- 继续定位：单独固定原生窗口后，展开头两帧仍有细缝，收起已无异常；HostingView 安全区实测为0。发现窗口仍使用默认出场动画，增加 animationBehavior == .none 回归，修改前断言失败；明确禁用系统动画，避免与自定义缩放叠加。
- 最终实机逐帧验收：禁用系统出场动画后，90帧录屏的顶部露底异常为0；旧版异常帧28/29，仅固定窗口的中间版异常帧32/33。已目视检查初始展开帧，展开和收起顶边均保持贴合，录屏保存到 build/visual-review/notch-animation-final.mov。
- 最终动画版本再次真实拖入：远离刘海的 (500,500) 持续拖动保持展开，投放成功；从暂存卡片拖回 Finder Destination4，cmp 与源文件一致。已清理本次暂存记录和测试窗口。
- 最终全量测试260项，256通过、4跳过、0失败；固定开发证书 Release 与包内签名验证通过，DMG VALID，CRC32 728E3997，已从该DMG安装到 /Applications/OneBoard.app。

## 2026-09-07 桌面浮窗可读性

日历与 Mac 状态使用随系统深浅色切换的不透明窗口底色，隔离桌面壁纸透色，并保留圆角、细边界及窗口阴影。日历非本月日期以次级文字和较轻字重区分，不对整格降低透明度；周数与 Mac 状态辅助信息提高可读性。改动仅作用于这两个浮窗，悬停、点击关闭和日历置顶行为保持原规则。

- 实机复现：桌面蓝色壁纸明显透入 ultraThinMaterial，日历蓝色选中项、非本月日期与状态指标对比不足。删除这两个浮窗的透明材质及日历透色渐变；辅助小字调整到11pt。增加深浅色渲染与背景alpha回归。
- 验证完成：全量261项测试，4项跳过、0失败；Release打包完成，DMG校验CRC32为0319F813，固定开发证书签名及安装校验通过。在桌面蓝色壁纸下实际打开日历和Mac状态，确认底色无透色、非本月日期及状态辅助信息清晰且未截断；深浅色渲染背景alpha回归通过。验证截图保存在build/visual-review/calendar-readable-desktop.png和mac-status-readable-desktop.png，验证后恢复原应用窗口显示。


## 2026-09-07 全应用视觉与动效统一

- 改动前先 commit + push：`3cffd4a`，远端 `origin/main` 已确认更新。
- 设置页、账号授权/编辑弹窗及剪贴板、翻译/OCR、网关、待办/历史、文件暂存、日历、Mac 状态、截图工具栏和长截图控制/预览使用同一套中性底色、蓝色强调与圆角规范。设置和工具外壳底色不透明，深浅色同时维护；卡片使用浅一级底色和细边界，不叠加渐变与装饰阴影。刘海暂存保留顶部内凹肩线及深色外壳，统一强调色并提高辅助文字对比。

- 通用控件采用 30pt 高度、8pt 圆角，卡片 12pt、面板 16pt；紧凑标注工具保留既有命中范围和布局。主按钮、次按钮、图标按钮与筛选项分别复用公共样式，辅助文字以 11–12pt 为主。模型和账号页添加操作位于滚动区外，编辑弹窗不再叠加表单左右缩进。

- 按 Design Taste 的一致性原则和 Finesse 的反馈/动效规范实现，使用现有 SwiftUI/AppKit。悬停反馈为 150ms；刘海与待办面板进入 300ms、退出 200ms，退出快于进入，待办滑动距离收敛到 12pt；系统“减少动态效果”下取消相关位移/缩放。待办完成后立即变淡并保持勾选反馈，沿用原有完成时序。保留原生焦点、悬停阈值、日历置顶、文件拖放及截图会话规则，不改变截图原图、标注颜色与账号授权边界。

- 验证：桌面环境全量 262 项，4 项按需渲染跳过，0 失败；工具/设置深浅色及编辑弹窗独立进程渲染通过。初次沙盒运行因无法访问系统剪贴板与 NSScreen 出现 8 项断言失败，桌面环境重跑已通过。
- 渲染边界：原生 List 在离屏缓存中未绘制内容，待办行单独渲染检查；尝试附着窗口并推进 RunLoop 后，未打包 xctest 的通知中心触发 bundleProxy 异常，已恢复无生命周期副作用的渲染方式。不能据此宣称全流程手动验收。
- 输出：`build/unified-ui/`。Release 构建通过，使用固定 Apple Development 证书 `525E49180EC9AE646202F2444C14D65C2E75E4A1` 签名并完成包内签名检查。`build/OneBoard.dmg` 校验 VALID，CRC32 `5C5E195A`。本轮未替换已安装应用，UI 改动保留在工作区供用户查看效果。
- 最后补齐授权引导浮窗的公共外壳，扩大到 280×140pt 以容纳统一按钮，深浅色渲染无截断；最后重新运行全量 262 项（4 跳过、0 失败）、工具渲染与固定证书 Release 打包，DMG 校验 VALID。


### 2026-09-07 日历与 Mac 状态图标生命周期

日历与 Mac 状态的菜单栏开关分别更新自己的图标；关闭时同步解除对应悬停监听、停止计时器并收起对应浮窗。三个菜单栏项统一在极简入口模块创建并保持实例，开关仅切换各自 isVisible；绑定在主线程同步完成，避免快速开关后的延迟绑定，保留另一个图标及其设置。原生回归覆盖两个方向的关闭、重新开启和快速连续开关。实际设置点击后的图标显示仍需桌面验收。

拖动检测仅解析当前系统文件 URL 载荷，不读取文件属性；旧式文件列表解析也不自动探测目录。普通文件校验及缩略图读取留在实际投放后，避免未投放文件就触发受保护文件夹授权。完全磁盘访问可由用户在系统设置中自愿授予，应用不能自行开启，也不能替代截图和辅助功能权限。

验证：拖动检测新增回归修复前失败、修复后通过；菜单栏生命周期回归修复前有 2 个断言失败，修复后通过。最终全量 264 项测试、4 项跳过、0 失败；Release 构建和固定开发证书签名完成，DMG 校验 VALID（CRC32 92EB661D）。桌面自动化读取 OneBoard 超时，不能据此宣称真实图标显示与系统授权提示已完成手动验收。


### 完全磁盘访问管理

设置 → 授权新增完全磁盘访问管理及当前应用 Finder 定位入口，状态以系统设置为准，不伪造开关或读取受保护文件探测授权。文件暂存与剪贴板文件预览实际遇到权限拒绝时引导前往系统授权，每次运行最多提示一次；取消后仍可从设置主动管理。缺失文件、磁盘空间和网络错误不触发权限引导。完全磁盘访问可选，由用户在系统中开启，之后重启应用并重试；不替代截图、辅助功能或备份目录选择。

本次验证：265 项全量测试、4 项跳过、0 失败；Release 构建及固定开发证书签名完成。系统中的最终授权开关仍由用户操作，未自动修改系统权限。


## 日历滚轮切月与截图工具栏避让

- 新增日期区滚动监听：滚轮一次一月；触控板累积 12pt 后切一月，同一手势和惯性不重复触发，右侧列表不受影响。
- 全屏工具栏以每屏 visibleFrame 的局部坐标避开 Dock，上下空间均不足时回退到底部内侧，保留 12pt 间距。
- 微信与日历窗口漏选仍待现场诊断；现有候选按 WindowServer 前到后排列，不能以面积排序代替真实遮挡关系。本次未修改候选算法，也不宣称漏选问题已解决。
- 桌面窗口几何读取的扩展执行连续两次自动审批超时；默认命令会话返回空窗口列表，无法作为微信窗口信息的证据。

Mac 状态卡片移除 ScrollView，不再滚动，窗口与内容高度由 600pt 增至 660pt，完整展示网络、三条进程用量、性能、存储与电池信息；深浅色渲染按新高度验收。

### 本轮验证结果

- 最终全量测试：269 项，4 项跳过，0 失败（桌面执行环境）；默认沙盒缺少屏幕和拖放服务的首次失败不作为产品缺陷。
- 相关定向回归及深浅色卡片渲染：25 项通过；三条进程用量、存储、电池和运行时间在 660pt 卡片内完整显示。
- Release 构建及固定 Apple Development 证书签名完成，最终包为 build/OneBoard.dmg。
- 微信/日历窗口漏选尚未修复，需获得窗口几何诊断执行许可后继续定位；未修改候选顺序算法。


## 窗口预选漏选后续修复

- 用户明确批准窗口元数据读取后，现场发现程序坞拥有 1710×1112、layer 20、alpha 1 的全屏窗口，出现在普通窗口和日历浮窗前面；即使使用 excludeDesktopElements 仍会返回。旧逻辑会先命中它。
- 每次截图按 com.apple.dock 解析当前 PID 并排除该进程的候选，避免依赖“程序坞”本地化名称或固定 PID；不排除其他应用同层浮窗。
- 新回归覆盖微信/日历在桌面上和近全屏窗口上四种组合，修复前产生 8 个失败断言，修复后通过；23 项截图相关测试通过。
- 界面工具选择微信被自动审批拒绝：可能读取私人辅助功能内容，超出用户仅允许窗口元数据的范围。后续使用已获准的几何信息验证，不将真实截图操作宣称为已验收。

### 窗口预选最终验证与文档整理

- 直接编译项目 ScreenshotWindowCandidate 做现场元数据复测：动态解析 Dock PID，候选由 26 个变为 25 个；屏幕中心由 (0,0,1710,1112) 的 Dock 层变为 (0,65,1710,1008) 的实际应用窗口，snapshot 实际入口结果一致。
- 初次探针使用“过滤后不能有任何全屏矩形”的过强断言失败；合法全屏应用本应保留。后续按窗口来源与过滤前后命中顺序核对，不修改产品的真实前后顺序规则。
- 最终全量测试 270 项、4 项跳过、0 失败；Release 与固定开发证书签名完成，新 build/OneBoard.dmg 通过 hdiutil verify。
- 按用户要求重写 README（304 行归并为 154 行），按功能、使用、权限、数据和构建组织；AGENTS.md（207 行归并为 133 行）按模块维护当前约束，保留 CodeGraph 初始化许可、截图会话、拖放、身份验证、凭据隔离、备份和发布要求。
- 更新旧 README 的字号循环、自动长截图、默认 CloudKit、固定 5 小时额度和输入监听等过时描述；主程序最低版本按 Package.swift 的 macOS 15.0 表述，AGENTS 记录资源仍声明 14.0 的差异，本次不改构建配置。
- 两份文档的本地链接检查通过。打包文档的 ad-hoc 说明统一为不安装的临时构建，避免与固定证书规则冲突。
- 微信、日历真实鼠标截图尚待安装验收；窗口元数据及注入前景窗口的回归通过，不代表界面自动验收已完成。

## Mac 状态磁盘容量单位修复

- 实机文件系统总容量 494384795648 字节，旧 `.memory` 格式显示 460.43 GB；空闲 216584196096 字节显示 201.71 GB。根卷与数据卷本次读数一致，确认单位口径错误。
- 磁盘总容量与空闲容量改为 `.file` 十进制格式，分别为 494.38 GB / 216.58 GB；内存格式保持不变。文件系统空闲不含可清理空间，界面改为“空闲”并补充提示；系统 important-usage 查询本机返回 0，未将其用作容量来源。
- 新增容量格式回归，先以旧格式运行得到两条断言失败，修复后 MacStatusTests 2/2 通过。沙盒全量运行在菜单栏测试中未输出完整总结；桌面环境重跑 271 项、4 项按需渲染跳过、0 失败。Release 构建成功。
- 使用现有 Apple Development 证书打包，build/OneBoard.dmg 完整性验证通过。尚未安装并人工核对菜单栏显示。
- ProxySidecar 共约 7.0 GB，几乎全部为被 Git 忽略的 target 缓存（debug 4.8 GB、release 2.2 GB）；源码及 Cargo 文件必须保留。后续按用户要求已删除 target，目录剩余约 208 KB；下次构建会重新生成缓存。

### 日历休班角标与剪贴板权限引导

- 修复单数字日期的 overlay 将“休”压窄：角标固定 14×14pt，并拒绝压缩；浅色、深色日历渲染均检查通过。
- 剪贴板记录及复制文件引用不再读取文件属性。已知 Containers / Group Containers 路径先展示完全磁盘访问入口，由用户继续后生成缩略图；不声称路径识别能检测授权，也不探测受保护文件。
- 发现后台 `resourceValues` 读取这一潜在系统授权触发路径，但未在安装应用中复现原始 TCC 弹窗，不能宣称已确认唯一根因或完全消除弹窗。
- 最终 FileAccessPermissionTests：3 项通过；FeaturePanelRenderTests：1 项通过，输出日历深浅色图。
- 全量测试尝试存在 FileDropTargetTests / FileShelfDropSessionTests 失败，包括 NSScreen 为 nil，且在菜单栏测试中未产生完整汇总；不报告全量通过。本次最初的 bundle 类型分类断言失败已修正并定向重跑通过。
- 安装后的真实系统权限与鼠标交互待人工验收。
- 随后在真实 macOS 会话重跑最终全量测试：273 项、4 项跳过、0 失败。此前 NSScreen/原生拖放失败未在该运行中复现；Release 构建通过。渲染与自动化结果不替代安装后 TCC 实测。
- 标准 DMG 已生成，应用沿用本机 Apple Development 身份签名，打包内严格签名检查通过；`hdiutil verify build/OneBoard.dmg` 通过。首次 DMG 创建受设备访问限制，真实 macOS 会话重试成功。
