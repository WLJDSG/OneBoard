# OneBoard 开发约定

本文件维护当前有效的工程约束。功能介绍见 [README](README.md)，具体变更和验收结果写入[开发日志](开发日志/)。新增规则应并入对应章节，替换已经失效的描述，避免按日期重复堆叠。

## 1. 项目与工作方式

- 技术栈：Swift 5.9+、SwiftUI + AppKit、SQLite / GRDB、SwiftPM；内置代理位于 `ProxySidecar/`，使用 Rust/Cargo。
- SwiftPM 根目录：`OneBoard/`；主 Bundle ID：`com.oneboard.mac`。当前 `Package.swift` 主程序目标为 macOS 15.0；资源 Info.plist 仍声明 14.0，不能据此宣称 macOS 14 已验证兼容。调整最低系统版本时同步检查主程序、扩展、Helper 与资源声明。
- `App_minimal/` 只创建并持有菜单栏项、启动应用；全部业务代码放 `OneBoardKit`（`App/`、`Core/`、`Modules/`、`Shared/`）。不要合并入口与业务模块，以免引入 macOS 26 的 NSStatusItem 渲染回归。
- 开工先确认 cwd、仓库、分支及工作区已有改动。先找根因，说明必要假设；只修改请求范围，不清理无关代码，不覆盖用户未提交的工作。
- 采用 Swift 命名惯例、中文说明性注释；UI 遵守 `@MainActor`，数据库遵循现有异步接口与事务约定。保留仍有效的迁移和协议兼容。
- 低风险、可逆且已获授权的实现选择自行推进；业务语义、数据安全或外部影响无法从上下文确定时，只暂停依赖该信息的步骤。

## 2. 代码检索与技能选择

- 有可用 CodeGraph 索引时，结构问题优先用它：符号定位用 `codegraph_search`，区域背景用 `codegraph_context`，调用路径用 `codegraph_trace`，多个符号源码用一次 `codegraph_explore`，影响分析用 `codegraph_impact`。
- 不用多轮 grep 或重复读取重建已返回的结构；架构问题直接分析，不委派重复探索。文字、日志、注释查询用 `rg`；索引不可用、不完整或与当前证据冲突时可回退原生检索。
- `.codegraph/` 未初始化时，先询问是否运行 `codegraph init -i`。初始化必须获得许可，但不得阻断可以通过源码阅读完成的工作。
- 先判断任务类型，再选最窄、直接相关的技能；不因关键词重合加载通用流程，不重复叠加技能。普通修改沿用项目栈，不自动引入新依赖。
- 用户指令与当前项目约定优先于技能默认建议；技能安装不代表允许修改其他技能。

## 3. 截图与标注

### 捕获、候选与会话

- 普通截图按 `NSScreen.screens` 为每屏独立执行 `screencapture -D`、创建遮罩并裁剪。不得先拼合不同缩放比例的屏幕，再按主屏尺寸解释像素；屏幕坐标和位图坐标分别换算。
- 窗口候选必须在遮罩出现前获取，保留 WindowServer 前到后顺序并转换为各屏局部坐标。按 `com.apple.dock` 解析当前 PID，排除程序坞的全屏交互层；`excludeDesktopElements` 不足以排除它。不得按窗口面积排序，或统一排除所有 layer 20 / 第三方浮窗。
- 悬停只预览，单击选窗口；按下及 4pt 内抖动保持预选，超过阈值才进入自定义拖动。键盘事件只交给所属遮罩。
- 普通截图遮罩以同一张每屏截图作为不透明背景；不得用 `blendMode.clear` 挖出会穿透鼠标的选区。
- 调整与标注阶段复用完整 `AnnotationToolbarView`；选择工具立即锁定选区并安装画布。接受首次点击，工具栏区域的鼠标序列不能重置选区。
- 工具栏优先放选区下方、其次上方；两侧均放不下时放在本屏局部 `visibleFrame` 底部上方 12pt，避开 Dock 和刘海。
- OCR、翻译、复制、保存、贴图统一经过截图会话完成路径，先关闭所有屏幕遮罩和监听。翻译入口保持可见，贴图保留原始位图及选区初始逻辑尺寸。

### 标注交互

- 文字和框选文字字号限 15–25，每次增减 1，到边界停止；工具栏内联显示，± 按钮的整个 22×28pt 范围可点击。
- 工具色板不因马赛克切换而隐藏；只有数值调节器使用底框，并按工具标注字号、线宽、编号、颗粒。
- 新建和双击编辑文字复用原位输入器，不绘制材质底和整圈边框；单击选中后保留移动柄与右下缩放点。文字拖动使用固定画布坐标，框选标注作为单层撤销。
- 编号保持 1...N 连续，点击已有标记调整顺序；编辑、删除和重排可撤销。
- 马赛克预览与导出共用原图固定像素块，拖动预览不透明。取色使用每屏原始截图的实时 sRGB，Command+C 复制。

### 手动长截图

- 等待用户手动滚动、完成或取消；首帧与后续帧均使用同一 ScreenCaptureKit 路径，排除自身窗口，不能反复隐藏边框。
- 每屏一层选区外透传暗罩，选区挖空；不启用阴影，不用四块全屏窗口拼成边框。范围只画四角短标记，保留完成栏和预览，不绘制贯穿边线。
- 无可靠重叠或匹配有歧义时不追加；低纹理文档不可仅凭横向纹理判定。

## 4. 菜单栏、日历与 Mac 状态

- OneBoard、日历、Mac 状态三个 NSStatusItem 在极简入口创建并持有；图标统一 18pt 模板画布。网速两行不得改变基线或遮挡 CPU/内存文字，网关使用局域网图标。
- 两个独立图标的开关只切各自 `isVisible`；绑定在主线程同步完成。关闭时解除对应悬停监听、停止计时器并收起浮窗，保留另一个图标及其设置。覆盖双向关闭、重开和快速连续切换。
- 日历/Mac 状态连续悬停 700ms 打开，离开重置；点击立即切换。移出图标与卡片 350ms 关闭，再次点击关闭后抑制原地 hover 重开。
- 日历置顶状态和窗口层级由同一控制器管理；固定六行、两侧栏头与筛选行等高。非本月日期用次级文字和较轻字重，不能对整格降透明度；农历/节气用 LunarSwift，未来未公布调休不推算。
- 日历休/班角标固定为 14×14pt，不接受日期文字的窄宽度压缩。
- 日历左侧日期区滚轮每次一月、触控板每手势最多一月，忽略惯性；不截获右侧列表滚动。
- 磁盘容量使用十进制，内存使用二进制；文件系统空闲容量标明“空闲”，不冒充包含可清理空间的系统可用容量。
- Mac 状态不提供纯图标模式；卡片为 400×660pt，移除滚动容器，完整展示三条进程用量及底部信息。不伪造传感器值，进程流量标明采样以来累计。
- 两个浮窗使用随系统深浅色变化的不透明底色，保留圆角、细边界和窗口阴影，辅助文字保持可读。

## 5. 文件暂存、剪贴板、Finder 与待办

- 文件拖动通过无权限轮询识别本次 `.drag changeCount` 与系统文件 URL 载荷，不把旧载荷或普通窗口拖动当成文件会话。检测时不读取文件属性，旧式文件列表也不探测目录；普通文件校验和缩略图读取留到投放后。
- 暂存兼容 Finder file URL 和旧式文件列表，完整投放区域使用原生接收。拖出写 NSURL，不用内容型 NSItemProvider 或纯文本路径冒充文件拖拽。回归必须验证 HostingView 命中及真实 AppKit 拖放回调，不能只测 hitTest。
- 刘海触发区覆盖系统实际刘海宽度及顶边，不向两侧扩张，底部仅加 2pt 容差；普通悬停连续 0.5 秒，计时器覆盖 common RunLoop。有效文件拖动期间持续展开，松手后按鼠标位置恢复收起；不恢复右上角收起箭头。
- 暂存原生窗口固定贴住屏幕顶边，禁用系统默认出场动画；仅内容从顶部中央缩放，展开/收起都不透明，保留顶部内凹肩线。AirDrop 期间持有分享会话，由系统让用户选择收件人。
- 剪贴板单击预览，双击向原应用复制粘贴；缩略图后台生成、限量缓存，绘制路径不遍历全文或解码原图。文件用系统 Quick Look，不手动关闭 SwiftUI 正在销毁的 QLPreviewView。
- Finder Sync 只生成 `oneboard://new-file` / 打开终端请求，实际操作由主应用执行。桌面目录覆盖本地 Desktop、Finder 映射的 iCloud 根容器、iCloud Desktop、系统解析目录和符号链接目标；目录集合与沙盒权限同步修改。
- iCloud 桌面通过主应用独立新建入口提供服务，不以扩大 Finder Sync 注册范围宣称突破 File Provider 限制。后缀限制长度并拒绝路径分隔符；自定义后缀只代表空文件，专有格式使用有效模板。
- 待办完成后立即变淡并保留勾选反馈，沿用既有完成时序。

## 6. 账号、模型配置与用量

### 凭据及切换边界

- API Key、供应商元数据、OAuth 缓存、额度、订阅和重置次数存入 `~/Library/Application Support/OneBoard/oneboard.sqlite`，不接入 macOS Keychain，不收集密码或验证码。
- Codex 新账号使用官方浏览器 OAuth（PKCE、state、localhost 回调）和标准授权端点；不套用 Desktop 私有中转页或伪装专用参数。
- 切换账号前先验证并续期目标凭据，再退出 Codex；确认主进程与旧 `app-server` 都结束后，才替换认证存储并重开。服务端未轮换 refresh token 时保留旧值；当前账号正在 Codex 中运行时不得并发刷新。
- 模型切换与账号切换隔离：Codex 仅合并模型相关 `config.toml`，不改 `auth.json`；自定义供应商同步受管 `model_catalog_json`，切回官方移除覆盖。运行中同样必须先退出并确认旧进程结束，写入后重开。
- Claude Code 仅合并 `settings.json.env` 受管键，默认及 Haiku/Sonnet/Opus/Fable/子代理槽位独立建模。切换前保留未知字段、生成稳定备份并原子写入；活动配置保存后同步重载代理。
- Claude OAuth 原件存 SQLite，仅向 Claude Code 物化 access token；切换第三方时清除，不得流入翻译或代理。独立账号页只展示自身操作错误，不串入 Codex 的供应商切换状态；支持添加、切换、编辑、重新授权和删除，新会话生效。
- Codex 与 Claude 授权页统一标题、待授权账号、OAuth 卡片、等待、链接复制及取消交互；账号输入放弹窗内，Claude 保留授权码回填。
- 代理仅在内存接收 Key，受管客户端配置只写占位认证；正常退出刷新尾部统计并清理，父进程强退/崩溃后代理自行停止、释放端口。

### 模型目录、额度与统计

- 模型目录只用当前连接和 API Key 返回结果；连接改变时清空旧目录并拒绝过期响应，保留模型独立的 1M 标记。常用供应商协议自动识别，高级兼容默认折叠。
- 供应商模板自动配置端点；完整推理 URL 与模型目录地址分别解析。额度自动/手动配置按接口区分缓存；DeepSeek 官方余额固定源站 `/user/balance`，不继承 `/anthropic`。
- Codex 额度按实际窗口时长分类，不将 primary 固定标为 5 小时。第三方额度支持 DeepSeek/Sub2API/SiliconFlow/OpenRouter；其他同源地址尝试 Sub2API `/v1/usage`，保留手动接口选择。
- 只展示可信快照；失败显示 HTTP 状态与服务端原因、旧快照时间。不执行导入的第三方用量脚本，不伪造余额；未接入额度不能描述为凭据或代理失败。
- Token 按供应商源和 API Key 分组，相同 Key 的 Codex/Claude 配置共享统计，换 Key 分开计算；展示当日、累计、缓存命中及输入/输出/缓存写入明细。
- 供应商历史快照与本地统计分别展示，不能合计或由余额推算历史 Token。本地统计仅含启用后通过代理/翻译的请求，以本机自然日计数，缓存 Token 不重复相加。
- 代理 stdout 只输出不含正文/Key 的计数事件；主应用写入 `ai_usage_events` 并按请求 ID 去重。异常强杀可能丢失尚未输出的内存事件，不能声称零丢失。
- 翻译默认和临时配置复用供应商模型、连接及协议，支持 Chat Completions、Responses、Anthropic Messages、Gemini；不切换客户端活动配置，不重复维护独立 DeepSeek Key。选中文字前检查辅助功能，空选区静默返回。

## 7. 权限、网关与备份

- 不恢复已移除的输入监控监听、检测和设置入口，或没有实际 Finder Apple Events 调用的自动化声明；卸载仍兼容历史授权记录。保留仍有用途的截图、辅助功能、提醒和用户目录授权。
- 网关切换、Helper 卸载、App 内彻底卸载必须先 `deviceOwnerAuthentication`（Touch ID 优先、登录密码回退）。首次安装/升级 Helper 与初始白名单写入共用一次管理员授权。
- 网关先检查 Helper 和当前用户授权；缺少时走既有安装授权流程，再进行设备身份验证。Helper 缺失或白名单拒绝不得回退管理员 shell；验证中失焦或外部点击不能关闭网关面板。
- Helper 状态含子进程检查，`SystemCapabilityViewModel` 初始化和刷新必须后台执行，不在主线程单例初始化中 `waitUntilExit`。设置首次打开回归必须包含独立进程冷启动。
- 完全磁盘访问可选且由用户在系统中开启，状态以系统设置为准；保留管理与 Finder 定位当前 App 入口。不得通过读取受保护文件探测授权，也不能替代截图、辅助功能或备份目录选择。
- 剪贴板后台记录及复制文件引用不读取文件属性；已知其他 App 容器路径的预览先显示完全磁盘访问管理入口，主动继续后才读取，不将路径判断当作授权状态。
- 暂存/剪贴板文件预览真实遇到权限拒绝时，每次运行最多引导一次；缺失文件、空间和网络错误不触发权限引导。取消后仍可从设置管理，系统开启后重启并重试。
- iCloud 当前为 `app/oneboard` 明文配置备份，用于重装恢复；明确包含 Key/凭据。保留旧加密实现兼容，`local_sync` 密钥不导入导出；无合法 profile 不声明 CloudKit entitlement。
- 目录书签只留本机，不在启动时探测未授权 iCloud 目录。规范化路径校验忽略末尾斜杠，行内显示错误；选择目录后立即重试已开启备份，异步读写全程保持安全作用域。
- 备份失败、未下载或读取失败不得覆盖备份；无变化不发布进度、不重写。目录授权使用统一的已连接、更改、断开操作。

## 8. 视觉、设置与快捷键

- 沿用 SwiftUI/AppKit 和公共设计组件。设置、编辑/授权弹窗及工具面板使用中性不透明底色、蓝色强调、浅一级卡片和细边界，深浅色同时维护；不叠加渐变和装饰阴影。截图冻结原图、标注颜色和刘海深色外壳保持各自语义。
- 复用 `SettingsDesign` / `FeaturePanelDesign`，避免局部设置改动意外改变截图遮罩；刘海保留内凹肩线与辅助文字对比。普通控件 30pt 高、8pt 圆角，卡片 12pt、面板 16pt；紧凑标注控件保留既有命中范围。
- 主按钮、次按钮、图标按钮、筛选项分别使用公共样式，辅助文字以 11–12pt 为主；模型/账号添加按钮位于滚动区外，编辑弹窗不重复叠加表单左右缩进。
- 悬停反馈 150ms；刘海/待办进入 300ms、退出 200ms，待办位移 12pt；系统减少动态效果时取消相关位移/缩放，保留原生焦点和即时点击反馈。
- 设置按日常工具、连接与账号、应用分组；OCR/翻译、待办/文件各自独立，菜单提供日常工具入口，维护动作放子菜单。设置内容不得拖动窗口。
- AI 供应商与 Codex 账号支持拖拽排序；验证本列表系统载荷，只在成功投放时持久化，取消还原。编辑菜单保留系统复制粘贴。
- 全局快捷键只由 `QuickLaunchBindings` 注册，固定 Option＋字母/数字；迁移保留占用，不重新注册旧快捷键。选择目标后立即绑定。

## 9. 验证、打包与文档

代码修改后从 SwiftPM 根目录测试和构建，再从仓库根目录打包：

```bash
cd OneBoard
swift test --disable-sandbox
swift build -c release --disable-sandbox
cd ..
bash script/package_app.sh
hdiutil verify build/OneBoard.dmg
```

- 修 bug 先补能复现症状的回归，再验证最终代码；不要把旧测试结果归于新修改。桌面依赖测试须在有 WindowServer 的 macOS 会话运行；跳过、环境失败和产品失败分别记录。
- 真实 hover、点击、拖放、贴图清晰度、权限和首次打开体验不能只靠几何/渲染测试宣称验收完成；受限时明确尚待人工验证的项目。
- 标准交付 `build/OneBoard.dmg`，必须完成 `hdiutil verify`。正式安装测试保持同一开发证书，不将 ad-hoc 签名造成的 TCC 变化解释为普通重启问题已解决。
- 独立开发包：`ONEBOARD_BUNDLE_ID_SUFFIX=.dev2 bash script/build_app_bundle.sh`。卸载通过 App 菜单、DMG 的 `卸载残留.command` 或 `bash script/uninstall.sh`。
- 行为变化同步 README、对应需求/技术/设计/开发步骤文档、本规则文件及当日开发日志。README 描述当前用法，本文件记录约束，测试数量、诊断过程和历史方案只写日志。
- 最终说明修改、原因、验证结果、安装包及尚未完成的验收。打包细节见[打包与修复流程](docs/打包与修复流程/README.md)。
