# OneBoard Runtime Fixes Plan

1. Replace private settings selector with an AppKit settings window controller.
2. Fix screen recording permission checks and make screenshot overlay become key/visible.
3. Restore drag shake detection with drag pasteboard file validation.
4. Add SwiftPM app bundle and DMG packaging scripts with Applications symlink.
5. Run Swift build and packaging verification.
